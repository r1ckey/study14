// AUTO-GENERATED BUNDLE FROM MARKDOWN FILES
// To update, run bundler.py

const studyData = {
    "title": "SQL Mastery",
    "subtitle": "Architecture & Practical Playbook",
    "localStorageKey": "sql_mastery_progress_v3",
    "themeColor": "#06b6d4",
    "secondaryColor": "#3b82f6",
    "axes": [
        {
            "title": "Phase 1: Design Philosophy",
            "modules": [
                {
                    "id": "1.1",
                    "title": "Output Driven Architecture",
                    "file": "Phase1/1.1_Output_Driven_Architecture.md"
                },
                {
                    "id": "1.2",
                    "title": "Logical vs Physical Design",
                    "file": "Phase1/1.2_Logical_vs_Physical_Design.md"
                }
            ]
        },
        {
            "title": "Phase 2: Cleansing & Prep",
            "modules": [
                {
                    "id": "2.1",
                    "title": "Handling Duplicates (ROW_NUMBER)",
                    "file": "Phase2/2.1_Handling_Duplicates_ROW_NUMBER.md"
                },
                {
                    "id": "2.2",
                    "title": "Gap Filling (LEAD/LAG)",
                    "file": "Phase2/2.2_Gap_Filling_LEAD_LAG.md"
                },
                {
                    "id": "2.3",
                    "title": "Flattening Arrays (EXPLODE)",
                    "file": "Phase2/2.3_Flattening_Arrays_EXPLODE.md"
                }
            ]
        },
        {
            "title": "Phase 3: Transformation",
            "modules": [
                {
                    "id": "3.1",
                    "title": "Row to Column (PIVOT)",
                    "file": "Phase3/3.1_Row_to_Column_PIVOT.md"
                },
                {
                    "id": "3.2",
                    "title": "Time Series & Windows",
                    "file": "Phase3/3.2_Time_Series_and_Windows.md"
                },
                {
                    "id": "3.3",
                    "title": "Difference Extraction (EXCEPT)",
                    "file": "Phase3/3.3_Difference_Extraction_EXCEPT.md"
                }
            ]
        },
        {
            "title": "Phase 4: ETL & Tuning",
            "modules": [
                {
                    "id": "4.1",
                    "title": "MERGE INTO Anatomy",
                    "file": "Phase4/4.1_MERGE_INTO_Anatomy.md"
                },
                {
                    "id": "4.2",
                    "title": "SCD Type 2 Implementation",
                    "file": "Phase4/4.2_SCD_Type_2_Implementation.md"
                },
                {
                    "id": "4.3",
                    "title": "Performance Troubleshooting",
                    "file": "Phase4/4.3_Performance_Troubleshooting.md"
                }
            ]
        }
    ]
};

const moduleContents = {
    "1.1": "# 1.1: アウトプット逆算型（風音屋）アーキテクチャ\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **アウトプット逆算思考（Output-Driven Design）**:\n> クエリ設計において「手元にあるデータ（Input）」から書き始めるのではなく、最終的に必要な「BIツールの表示結果（Output）」を定義し、そこに至るための「加工プロセス（Process）」を逆順に設計する手法。\n>\n> **CTE (Common Table Expression)**:\n> `WITH` 句を使用して定義される一時的な結果セット。クエリを論理的なモジュールに分割することで、可読性、デバッグ性、そしてオプティマイザによる最適化効率を向上させる「クエリの構造化」の要。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### ❌ インプット駆動の罠（スパゲティSQL）\n初心者や「とりあえず動けばいい」エンジニアは、以下の順序でSQLを書きます。\n1. `FROM raw_logs` (とりあえず生データを読む)\n2. `LEFT JOIN master_table` (とりあえずマスタを括り付ける)\n3. `WHERE ... GROUP BY ...` (最後に無理やり集計する)\n\n**結果:** JOINしてから集計するため、膨大な中間データが発生し、Sparkクラスターが悲鳴を上げます（Shuffle爆発）。また、1つのクエリが数百行になり、誰もメンテナンスできなくなります。\n\n#### ✅ アウトプット逆算の革命\nプロのエンジニアは「ゴール」から設計します。\n1. **Output**: 「10代女性の月間ユニーク視聴者数」という表が欲しい。\n2. **Process**: 「視聴ログをユーザーID×月で事前集計」＋「マスタからデモグラ情報を取得」。\n3. **Input**: `view_logs` と `user_profiles` が必要。\n\nこの思考プロセスをコードに落とし込むのが **CTEモジュール設計** です。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph TD\n    classDef output fill:#06b6d4,stroke:#fff,stroke-width:2px,color:#fff;\n    classDef process fill:#3b82f6,stroke:#fff,stroke-width:2px,color:#fff;\n    classDef input fill:#1e293b,stroke:#06b6d4,stroke-width:1px,color:#94a3b8;\n\n    F([Output: BIダッシュボード用マート]):::output\n    \n    E[Process: 事前集計データとマスタの結合]:::process\n    D[Process: ユーザー単位でのログ事前集計]:::process\n    \n    A[(Input: View Logs)]:::input\n    B[(Input: User Profile)]:::input\n\n    F -.->|1. ゴールの定義| E\n    E -.->|2. 加工の因数分解| D\n    D -.->|3. 最小限の読込| A\n    E -.->|4. マスタの特定| B\n\n    A --> D\n    D --> E\n    B --> E\n    E --> F\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n```sql\n/* \n   名前: 01_monthly_user_metrics\n   目的: 広告配信最適化のための月次ユーザー行動集計\n*/\n\nWITH \n-- ==========================================\n-- 1. Input: 対象マスタの抽出（最小限の列に絞る）\n-- ==========================================\ntarget_users AS (\n    SELECT \n        user_id,\n        gender,\n        age_group\n    FROM user_profiles \n    WHERE status = 'active'\n),\n\n-- ==========================================\n-- 2. Process: 事前集計（Pre-aggregation）\n-- ※ JOINの【前】に集計することで、Shuffleデータ量を劇的に削減する\n-- ==========================================\naggregated_views AS (\n    SELECT \n        user_id,\n        date_trunc('month', event_timestamp) AS target_month,\n        COUNT(DISTINCT video_id)             AS unique_videos,\n        SUM(view_time_sec)                   AS total_view_time\n    FROM raw_view_logs\n    WHERE event_timestamp >= '2024-01-01'\n    GROUP BY 1, 2\n)\n\n-- ==========================================\n-- 3. Output: 最終成形（Final Assembly）\n-- ==========================================\nSELECT \n    u.gender,\n    u.age_group,\n    v.target_month,\n    v.unique_videos,\n    v.total_view_time\nFROM target_users u\n-- 集計済みの「軽い」データ同士を結合するため、メモリ負荷が極めて低い\nINNER JOIN aggregated_views v \n    ON u.user_id = v.user_id;\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **SELECTから書かない**: まずWITH句でインプットと中間プロセスを定義する。\n- **Pre-aggregation**: JOINの前にGROUP BYを済ませるのがプロの鉄則。\n- **SQLは「文章」である**: 上から下に読めば、設計思想が伝わるようにCTEを配置する。\n- **Catalystを信じる**: CTEによる分割はDatabricksのオプティマイザによって最適化されるため、可読性を優先して良い。\n",
    "1.2": "# 1.2: 論理設計 vs 物理実装（検証機能付きSQL）\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **論理設計 (Logical Design)**:\n> データの意味、関係性、ビジネスルールをSQL（CTE）の構造として表現すること。人間が読んで「何を計算しているか」を理解するための設計。\n>\n> **物理実装 (Physical Implementation)**:\n> Databricks（Spark Catalyst）がクエリを実行可能なプランに変換し、メモリアロケーションやシャッフルを制御すること。\n>\n> **ユニットテスト付きクエリ (Self-Testing Query)**:\n> クエリ本体のロジックとは別に、データの整合性（NULLチェック、一意性チェック等）を確認するためのCTEを隠し持った設計手法。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### 🧐 「長いSQLはパフォーマンスが落ちる」という迷信\n多くのエンジニアが「サブクエリやCTEを増やすと、その分一時テーブルが作られて遅くなる」と誤解しています。\n\n**事実は逆です。** \nDatabricksの **Catalyst Optimizer** は、人間が書いた「入れ子」や「分割」を全て解体し、最も効率的な実行計画（Physical Plan）に再構成します。\nそのため、人間は「パフォーマンスを気にしてネストを深くする」必要は一切なく、**「最も読みやすくデバッグしやすい粒度」**でCTEを分割すべきです。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph LR\n    subgraph Logical [\"論理設計 (人間目線)\"]\n        A[CTE 1: Filter] --> B[CTE 2: Aggregate]\n        B --> C[CTE 3: Join]\n        C --> D[CTE 4: Test]\n    end\n\n    subgraph Catalyst [\"Catalyst Optimizer (物理目線)\"]\n        E[Optimal Physical Execution Plan]\n    end\n\n    Logical -->|コンパイル| E\n    style E fill:#06b6d4,stroke:#fff,color:#fff\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ 自己テスト機能付きクエリの実装\n\nバグ調査のたびに `SELECT * FROM ... LIMIT 100` を挟み込むのは非効率です。設計の時点で「検証ポイント」をCTEとして用意しておきます。\n\n```sql\nWITH \n  base_sales AS (\n    SELECT * FROM silver.sales WHERE amount > 0\n  ),\n  \n  -- ★ 検証用ブロック (Production時には最終SELECTから外す)\n  test__check_duplicates AS (\n    SELECT transaction_id, COUNT(*) \n    FROM base_sales \n    GROUP BY 1 HAVING COUNT(*) > 1\n  ),\n  \n  test__check_null_users AS (\n    SELECT COUNT(*) AS null_count \n    FROM base_sales \n    WHERE user_id IS NULL\n  ),\n\n  final_agg AS (\n    SELECT user_id, SUM(amount) AS total_amt\n    FROM base_sales\n    GROUP BY 1\n  )\n\n-- 通常時\nSELECT * FROM final_agg;\n\n-- デバッグ時: 1行書き換えるだけで検証結果を確認できる\n-- SELECT * FROM test__check_null_users;\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **論理と物理を分ける**: パフォーマンスチューニングのためにコードの読みやすさを犠牲にしない。\n- **デバッグを「資産」にする**: 調査に使ったクエリを捨てずに `test__` CTEとして残しておくことで、ドキュメントの代わりになる。\n- **Catalystを味方につける**: `EXPLAIN` コマンドを使用して、自分の書いた論理構造がどのように物理最適化されたかを確認する習慣をつける。\n",
    "2.1": "# 2.1: 重複排除と最新値の取得（ROW_NUMBER）\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **Window関数 (Window Functions)**:\n> 通常の `GROUP BY` が行を「集約（圧縮）」するのに対し、Window関数は行の数を変えずに「特定の範囲（窓）」に対して計算を行う関数。 `OVER` 句を伴う。\n>\n> **ROW_NUMBER()**:\n> 指定された順序に従って、各行に 1 から始まる連番を振る関数。重複データの特定や、「各グループの最大/最小/最新」を 1 件だけ抽出する際に使用される「重複排除の王道」。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### ❌ インタラクション爆発の恐怖\n「ユーザーごとの最新の住所を取得したい」という要件に対し、初心者は以下のように書きます。\n1. `MAX(update_date)` で最新日を特定する。\n2. その日付と `user_id` で元のテーブルと再結合（JOIN）する。\n\n**欠陥:** もし同じ日に 2 回更新があると、JOINの結果が 2 行になり、売上データが 2 倍になる事故（データの二重計上）が発生します。\n\n#### ✅ ROW_NUMBER() による「絶対的な 1 件」の抽出\nWindow関数を使えば、全く同じ日付・時刻のデータが複数あっても、システム的に「どちらかを 1 番、どちらかを 2 番」と序列をつけ、確実に 1 行に絞り込むことができます。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph TD\n    subgraph Raw [\"Raw Data (重複あり)\"]\n        A[User-A | 2024-01-01 | Tokyo]\n        B[User-A | 2024-02-01 | Osaka]\n        C[User-B | 2024-01-15 | Kyoto]\n    end\n\n    subgraph Windowing [\"Window Process (ROW_NUMBER)\"]\n        D[\"PARTITION BY user_id\"]\n        E[\"ORDER BY date DESC\"]\n    end\n\n    subgraph Result [\"Result (rn=1)\"]\n        F[User-A | Osaka | rn=1]\n        G[User-B | Kyoto | rn=1]\n    end\n\n    Raw --> Windowing\n    Windowing --> F\n    Windowing --> G\n    Windowing -.-> H[User-A | Tokyo | rn=2]\n    style H fill:#f1f5f9,stroke:#cbd5e1,color:#94a3b8\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ 標準的な最新値取得\n```sql\nWITH numbered_addresses AS (\n  SELECT \n    user_id,\n    address,\n    updated_at,\n    -- user_idごとにグループ化し、更新日の新しい順に背番号を振る\n    ROW_NUMBER() OVER (\n      PARTITION BY user_id \n      ORDER BY updated_at DESC, created_at DESC -- 同時刻対策\n    ) AS rn\n  FROM silver.user_address_history\n)\n\n-- 常に「最新の1件」だけを安全に抽出\nSELECT * \nFROM numbered_addresses \nWHERE rn = 1;\n```\n\n#### 🚀 Databricks SQL プロの技 (QUALIFY)\nDatabricks SQLでは `QUALIFY` 句を使うことで、CTEなしで 1 行で書けます。\n```sql\nSELECT user_id, address, updated_at\nFROM silver.user_address_history\n-- WHERE句ではなくQUALIFY句でWindow関数の結果をフィルタリング\nQUALIFY ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY updated_at DESC) = 1;\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **GROUP BYの代わり**: 特定の条件下での「最新」や「最大」を 1 件だけ残したい時は、迷わず `ROW_NUMBER()` を使う。\n- **時刻の衝突に備える**: `ORDER BY` には `id` や `created_at` などのサブキーも指定し、順序を一意に固定する（Determinismの確保）。\n- **QUALIFYの活用**: Databricks環境なら、クエリを短く保つために `QUALIFY` を積極的に利用する。\n",
    "2.2": "# 2.2: 欠損穴埋めと時系列比較（LEAD / LAG）\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **LAG()**:\n> 現在の行から見て「前（過去）」の行のデータを取得する関数。1つ前の値と比較して増減率を出す際に多用される。\n>\n> **LEAD()**:\n> 現在の行から見て「次（未来）」の行のデータを取得する関数。イベントとイベントの「間隔（滞在時間など）」を算出する際に便利。\n>\n> **LAST_VALUE() + IGNORE NULLS**:\n> NULLが続くデータにおいて、最も近い過去の「非NULL値」で穴埋めをする、時系列クレンジングの強力な武器。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### 🔍 差分ログの落とし穴\n例えばIoTセンサーや会員ランクのデータ。\n「変更があった時だけ」ログが記録されるため、そのまま集計すると、データがない日の状態が「NULL」になってしまいます。\n\n**例:** \n- 1月1日: プレミアム会員\n- 1月10日までログなし\n- 1月11日: ゴールド会員にランクダウン\n\n1月5日のデータは「NULL」ですが、ビジネス的には「プレミアム継続中」であるべきです。これを「埋める」技術が求められます。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph LR\n    subgraph Gap [\"データ欠損 (Raw)\"]\n        A[10:00 - Active]\n        B[10:01 - NULL]\n        C[10:02 - NULL]\n        D[10:03 - Error]\n    end\n\n    subgraph Filling [\"穴埋めプロセス (LAST_VALUE)\"]\n        B -.->|過去から伝播| A\n        C -.->|過去から伝播| B\n    end\n\n    subgraph Result [\"クレンジング後\"]\n        E[10:00 - Active]\n        F[10:01 - Active]\n        G[10:02 - Active]\n        H[10:03 - Error]\n    end\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ NULLを直前の値で埋める (Forward Fill)\n```sql\nSELECT\n  event_time,\n  status,\n  -- IGNORE NULLS を指定することで、NULLを飛ばして最新の値を拾い続ける\n  LAST_VALUE(status) IGNORE NULLS OVER (\n    ORDER BY event_time ASC\n    ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW -- 最初から現在まで\n  ) AS current_status_filled\nFROM silver.sensor_logs;\n```\n\n#### ✅ 滞在時間を算出する (LEAD)\n```sql\nSELECT\n  user_id,\n  page_url,\n  visited_at,\n  -- 次のページの訪問時刻を取得\n  LEAD(visited_at) OVER (PARTITION BY user_id ORDER BY visited_at ASC) AS next_visit_at,\n  -- 滞在秒数を計算\n  unix_timestamp(LEAD(visited_at) OVER (PARTITION BY user_id ORDER BY visited_at ASC)) - \n  unix_timestamp(visited_at) AS stay_sec\nFROM silver.page_views;\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **IGNORE NULLS の魔法**: 複雑な `CASE WHEN` を書かなくても、Window関数のオプションだけで欠損補完が可能。\n- **滞在時間は未来を見る**: `LEAD` を使って「次のアクション」の時刻を現在行に持ってくることで、1行完結の差分計算ができる。\n- **枠（Frame）の定義**: `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW` を忘れると、予期しない範囲を計算対象にしてしまうので注意。\n",
    "2.3": "# 2.3: 配列データの展開（EXPLODE）\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **Nested Data (ネスト構造データ)**:\n> JSONやParquet形式でよく見られる、1つのカラムの中に「配列(Array)」や「構造体(Struct)」が含まれている状態。\n> \n> **EXPLODE()**:\n> 1つのカラムに含まれる配列の要素を、独立した「行」に分解（爆発）させるテーブル生成関数。非正規化データを正規化形式（1行1事実）に戻す際に不可欠。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### 🧐 なぜ「横」ではなく「縦」にするのか？\nログデータには `purchased_items: [\"Apple\", \"Orange\", \"Banana\"]` のように、購入商品が配列で入っていることがあります。\nこのままだと「Appleが何個売れたか」を集計するには、文字列検索（LIKEなど）が必要になり、非常に低速で不正確です。\n\n集計の鉄則は **「1つのセルには 1つの値だけを入れる」** こと。 `EXPLODE` を使って、3つのアイテムを 3行に分解することで、通常の `COUNT` や `GROUP BY` が使えるようになります。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph TD\n    subgraph ArrayInput [\"Input (1 Row)\"]\n        A[Order: 001 | Items: Apple, Orange]\n    end\n\n    subgraph ExplodeEngine [\"EXPLODE Engine\"]\n        B{Item 1: Apple}\n        C{Item 2: Orange}\n    end\n\n    subgraph OutputRows [\"Output (2 Rows)\"]\n        D[Order: 001 | Item: Apple]\n        E[Order: 001 | Item: Orange]\n    end\n\n    A --> B\n    A --> C\n    B --> D\n    C --> E\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ 配列の縦展開\n```sql\nSELECT \n  order_id,\n  user_id,\n  item_name\nFROM silver.orders\n-- 配列を縦に展開し、item_name というエイリアスを付ける\nLATERAL VIEW EXPLODE(item_list) AS item_name;\n\n-- Databricks SQL のモダンな書き方\n-- SELECT order_id, EXPLODE(item_list) FROM silver.orders;\n```\n\n#### ✅ 出現順序（位置）を維持する (POSEXPLODE)\n配列の「何番目にあったか」に意味がある場合（例：検索結果の順位など）に使用します。\n```sql\nSELECT\n  search_id,\n  rank + 1 AS search_rank, -- インデックスは0から始まるため+1\n  content_id\nFROM silver.search_logs\n-- pos (position) と content_id の両方を取り出す\nLATERAL VIEW POSEXPLODE(results_array) AS rank, content_id;\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **集計前の縦展開**: 配列内の要素を分析対象（GROUP BYのキーなど）にしたい場合は、まず `EXPLODE` で縦持ちに変換する。\n- **LATERAL VIEW**: 既存の列（order_idなど）を維持しつつ新しい列を生成する場合の標準的な構文。\n- **重複に注意**: 1行を複数行に増やすため、展開後に集計を行う場合は、分母の数（Order数など）が狂わないように `COUNT(DISTINCT order_id)` を使う等の配慮が必要。\n",
    "3.1": "# 3.1: 縦持ちから横持ちへの変換（PIVOT）\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **PIVOT**:\n> 特定の列の「値」を新しい「列名」として昇格させ、データを縦方向（正規化形式）から横方向（ワイド形式）に変換する操作。\n>\n> **集計（Aggregation）**:\n> PIVOT操作は本質的に集計を伴う。複数行のデータを 1行にまとめるため、必ず `MAX`, `SUM`, `COUNT` などの集計関数を指定する必要がある。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### 🧐 なぜ PIVOT が必要なのか？\nDBには「ユーザーID」「質問ID」「回答内容」という縦持ちでデータが入るのが一般的です（アンケートデータなど）。\nしかし、このデータを機械学習にかけたり、Excelでクロス集計したりする場合、「1ユーザー 1行」で、各質問がカラムになっている必要があります。\n\n**例:** \n- Input: `U1 | Q1 | Yes` / `U1 | Q2 | No`\n- Output: `U1 | Answer_Q1:Yes | Answer_Q2:No`\n\nPIVOTを使えば、複雑な `CASE WHEN` を何十個も書かなくても、宣言的に横持ち変換が可能です。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph LR\n    subgraph Vertical [\"Input (縦持ち)\"]\n        A[User 1 | Q1 | Yes]\n        B[User 1 | Q2 | No]\n    end\n\n    subgraph PivotProcess [\"PIVOT(question_id)\"]\n        C{Q1?}\n        D{Q2?}\n    end\n\n    subgraph Horizontal [\"Output (横持ち)\"]\n        E[User 1 | Yes | No]\n    end\n\n    A --> C\n    B --> D\n    C --> E\n    D --> E\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ PIVOT 構文による特徴量生成\n```sql\nSELECT *\nFROM (\n  -- 元となるデータを抽出\n  SELECT \n    user_id, \n    question_id, \n    answer_text \n  FROM silver.survey_results\n)\nPIVOT (\n  -- 1. カラムに入れたい「値」\n  -- ※1ユーザー1回答のみならMAXで問題なし\n  MAX(answer_text) \n  -- 2. カラム名にしたい「元の列の値」をリストアップ\n  FOR question_id IN (\n    'q01' AS q_purpose,\n    'q02' AS q_interest,\n    'q03' AS q_satisfaction\n  )\n);\n```\n\n#### ✅ なぜ集計関数(MAXなど)が必要か？\nPIVOTは内部で `GROUP BY user_id` （PIVOT指定以外の列での集約）を行っています。SQLのルール上、集約された行の値を表示するには、たとえ 1件しかなくても集計関数が必要です。\n\n---\n\n### 5. 【Key Takeaways】\n\n- **ワイドデータの作成**: 特徴量テーブルや、クロス集計レポートを作成する際の必須テクニック。\n- **列名の固定**: `PIVOT` 句の `IN` リストで指定した値のみがカラムになる（それ以外は無視される）。\n- **ダミー集計**: 文字列を横に並べるだけの時は `MAX()` を使うのが定石。\n",
    "3.2": "# 3.2: 時系列集計と移動平均（Window Frame）\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **移動平均 (Moving Average)**:\n> データの乱高下を抑え、トレンドを把握するために、指定された期間（過去7日間など）の平均値を逐次算出すること。\n>\n> **Window Frame (ROWS / RANGE)**:\n> Window関数において、「現在の行から前後何行（あるいは何日間）」を計算対象とするかを詳細に定義する構文。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### 🔍 「過去7日」の定義に注意\n単純に `AVG(sales) OVER(ORDER BY date)` と書くと、データの最初から現在行までの「累計平均」になってしまいます。\n「過去7日間」にするには、Windowの「枠（Frame）」を指定する必要があります。\n\nさらに、**「過去7行」**と**「過去7日間（日付）」**は異なります。\n売上が 0円の日（レコードがない日）がある場合、`ROWS` を使うと「データがある過去7日間」を拾ってしまうため、分析結果が歪みます。厳密な期間指定には `RANGE` を使います。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph LR\n    subgraph Timeline [\"時系列データ\"]\n        A[Day 1: 100]\n        B[Day 2: 120]\n        C[Day 3: 80]\n        D[Day 4: 150]\n        E[Day 5: 110]\n    end\n\n    subgraph Window [\"Window Frame (3-Day)\"]\n        F[\"[A, B, C]\"] \n        G[\"[B, C, D]\"]\n        H[\"[C, D, E]\"]\n    end\n\n    Window --> F_Avg[Avg: 100]\n    Window --> G_Avg[Avg: 116]\n    Window --> H_Avg[Avg: 113]\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ 過去7日間の移動平均（ROWS指定）\n```sql\nSELECT\n  order_date,\n  daily_revenue,\n  -- 過去6行 ＋ 現在行 ＝ 合計7行の平均\n  AVG(daily_revenue) OVER (\n    ORDER BY order_date ASC\n    ROWS BETWEEN 6 PRECEDING AND CURRENT ROW\n  ) AS moving_avg_7d\nFROM silver.daily_sales;\n```\n\n#### ✅ 厳密な過去7日間の移動平均（RANGE指定 / Databricks SQL）\n```sql\nSELECT\n  order_date,\n  daily_revenue,\n  -- 日付が欠損していても、カレンダー上の「過去7日間」を正しく計算\n  AVG(daily_revenue) OVER (\n    ORDER BY CAST(order_date AS TIMESTAMP) \n    RANGE BETWEEN INTERVAL 6 DAYS PRECEDING AND CURRENT ROW\n  ) AS strict_moving_avg_7d\nFROM silver.daily_sales;\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **トレンド把握**: 単日の数値に一喜一憂せず、移動平均でトレンドを掴むのがKPI分析の基本。\n- **欠損の考慮**: 土日祝日などデータがない可能性がある場合は、 `ROWS` ではなく `RANGE` を検討する。\n- **パフォーマンス**: Window関数は自己結合（SELF JOIN）よりも圧倒的に高速かつメモリ効率が良い。\n",
    "3.3": "# 3.3: 差分抽出とリスト比較（EXCEPT / ANTI JOIN）\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **集合演算 (Set Operations)**:\n> クエリの結果（集合）同士を、数学的に合成・比較する操作。 `UNION` (和), `INTERSECT` (積), `EXCEPT` (差) がある。\n>\n> **LEFT ANTI JOIN**:\n> 「左側のテーブルには存在するが、右側のテーブルには存在しない」行のみを抽出するJOIN方式。差分抽出や「退会者リスト」「未購入者リスト」の作成において、最もパフォーマンスが高い手法の一つ。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### 🔍 「今月だけの人」をどう探すか？\n「先月の顧客リスト」と「今月の顧客リスト」があります。\n- 先月: [A, B, C]\n- 今月: [B, C, D]\n\n「新規顧客」は D です。これを探すには、「今月 **EXCEPT** 先月」と書くか、「今月 を左、先月 を右にして **ANTI JOIN**」します。\n\n`EXCEPT` は全ての列が完全一致しているかを見ますが、 `ANTI JOIN` は `user_id` などの特定のキーだけを基準に比較できます。実務では 9割のケースで **ANTI JOIN** が好まれます。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph LR\n    subgraph A [\"Table A (今月)\"]\n        A1[ID: 1]\n        A2[ID: 2]\n        A3[ID: 3]\n    end\n\n    subgraph B [\"Table B (先月)\"]\n        B1[ID: 1]\n        B2[ID: 2]\n    end\n\n    A1 -- Match --> B1\n    A2 -- Match --> B2\n    A3 -- No Match! --> Result([ID: 3 が差分])\n\n    style Result fill:#06b6d4,stroke:#fff,color:#fff\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ LEFT ANTI JOIN による差分抽出 (推奨)\n```sql\n-- 昨日のスナップショットにはいたが、今日はいなくなった「退会者」を探す\nSELECT \n  y.user_id,\n  y.email\nFROM \n  silver.users_snapshot_yesterday y\n-- 右側に存在しないIDだけを残す\nLEFT ANTI JOIN \n  silver.users_snapshot_today t\n  ON y.user_id = t.user_id;\n```\n\n#### ✅ EXCEPT による完全一致比較\n```sql\n-- テーブル全体の定義（全カラム）に差分があるレコードを抽出する\n-- 監査やデータ移行後の検証に便利\nSELECT * FROM table_legacy\nEXCEPT\nSELECT * FROM table_modern;\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **引き算の思考**: 「〜していない人」「昨日より減ったデータ」を出す時は、まず ANTI JOIN を疑う。\n- **パフォーマンス**: 大規模データにおいて、 `NOT IN (SELECT ...)` や `NOT EXISTS` よりも `LEFT ANTI JOIN` の方がオプティマイザにとって理解しやすく、高速に動作する傾向がある。\n- **重複排除**: `EXCEPT` は結果から暗黙的に重複を削除（DISTINCT）するが、 `LEFT ANTI JOIN` は左側の行をそのまま維持する。意図に応じて使い分ける。\n",
    "4.1": "# 4.1: Upsert処理の決定版（MERGE INTO）\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **Upsert (Update + Insert)**:\n> レコードが存在すれば更新（Update）し、存在しなければ挿入（Insert）する操作。\n>\n> **MERGE INTO**:\n> 2つのテーブルを特定の条件（キー）で突合し、一致した場合と一致しなかった場合の複数のアクション（UPDATE, INSERT, DELETE）を 1つのトランザクション内で実行するSQL構文。Delta Lakeの核となる機能。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### 🧐 なぜ UPDATE ＋ INSERT ではダメなのか？\n1. `UPDATE ...` を実行\n2. その直後に `INSERT ...` を実行\n\nこの 2ステップ方式だと、1 と 2 の間にエラーが発生した場合、データベースの中身が「更新はされたが追加はされていない」という不整合な状態になります。\nまた、逐次処理は非常に低速です。 `MERGE INTO` はこれらを 1つの「アトミック（不可分）」な処理として実行するため、**冪等性（Idempotency）**が保証され、再実行が安全になります。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph TD\n    subgraph Source [\"Source (持ち込むデータ)\"]\n        S1[ID: A | 100円]\n        S2[ID: B | 200円]\n    end\n\n    subgraph Target [\"Target (既存テーブル)\"]\n        T1[ID: A | 80円]\n    end\n\n    subgraph MergeLogic [\"MERGE INTO Logic\"]\n        M1{A は一致?}\n        M2{B は一致?}\n    end\n\n    M1 -- \"YES (MATCHED)\" --> U[UPDATE: 80 → 100]\n    M2 -- \"NO (NOT MATCHED)\" --> I[INSERT: ID:B を追加]\n\n    U --> Final[(Target Updated)]\n    I --> Final\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ MERGE INTO の解体新書\n```sql\n-- ① 書き換え対象の「ターゲット」\nMERGE INTO silver.customer_master AS target\n\n-- ② 持ち込む「ソース」\nUSING (\n  SELECT id, name, status FROM bronze.raw_daily_customers\n) AS source\n\n-- ③ 何をもって同一とみなすか（マージキー）\nON target.id = source.id\n\n-- ④ 一致した場合：最新情報に更新\nWHEN MATCHED \n  AND (target.name != source.name OR target.status != source.status) THEN \n  -- ※値が変わった時だけ更新するのが、ログやコスト削減のコツ\n  UPDATE SET \n    target.name = source.name,\n    target.status = source.status,\n    target.updated_at = current_timestamp()\n\n-- ⑤ 一致しなかった場合：新規追加\nWHEN NOT MATCHED THEN\n  INSERT (id, name, status, created_at, updated_at)\n  VALUES (source.id, source.name, source.status, current_timestamp(), current_timestamp());\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **安全な再実行**: ジョブが途中で落ちても、 `MERGE` なら何度実行しても最終的な状態は同じになる。\n- **パフォーマンスの最適化**: ソースデータを絞り込んでから `USING` に渡し、 `MATCHED` 句にも変更検知条件（AND target.x != source.x）を入れることで、不必要な書き込みを避ける。\n- **ACIDの遵守**: トランザクションとして処理されるため、ハーフコミット（中途半端な更新）を防げる。\n",
    "4.2": "# 4.2: 履歴管理と変更履歴保持（SCD Type 2）\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **SCD (Slowly Changing Dimension)**:\n> 時間とともにゆっくりと変化する属性（住所、役職、会員ランク等）を扱うための設計手法。\n> \n> **SCD Type 2**:\n> 履歴をすべて保持する手法。既存の行を上書きせず、新しい行を追加し、各行に「有効期間（開始日、終了日）」と「最新フラグ」を持たせる。過去のある時点での属性を正確に再現するために必須。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### 🧐 なぜ履歴が必要なのか？\n例えば、あるユーザーが 3月に「通常会員」で 100円の買い物をし、6月に「プレミアム会員」になったとします。\nもし Type 1（上書き）で管理していると、過去の 3月の売上を見ようとした時、当時のランクが「プレミアム」だったと誤認してしまいます。\n\nこれを防ぐために、「3月時点では通常だった」という**過去の事実を残しておく**のが SCD Type 2 です。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph TD\n    subgraph State_1 [\"Before (2023)\"]\n        A[ID: 1 | Rank: Regular | Valid: 2022-01-01 to 9999-12-31 | Current: Y]\n    end\n\n    subgraph Change [\"Update (2024)\"]\n        B[ID: 1 | Rank: Premium]\n    end\n\n    subgraph State_2 [\"After (2024)\"]\n        C[ID: 1 | Rank: Regular | Valid: 2022-01-01 to 2024-06-01 | Current: N]\n        D[ID: 1 | Rank: Premium | Valid: 2024-06-01 to 9999-12-31 | Current: Y]\n    end\n\n    State_1 --> Change\n    Change --> State_2\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ Delta Live Tables (DLT) による自動実装\nSCD Type 2 を手書きの SQL で実装しようとすると、古い行のクローズと新しい行の追加を同時に管理する必要があり、非常に複雑でバグが起きやすいです。Databricksでは、DLTの機能を使って宣言的に記述できます。\n\n```python\nimport dlt\n\n# SCD Type 2 の履歴保持テーブルを定義\ndlt.create_streaming_live_table(\"dim_customers_scd2\")\n\ndlt.apply_changes(\n  target = \"dim_customers_scd2\",\n  source = \"customer_cdc_stream\",\n  keys = [\"user_id\"],\n  sequence_by = dlt.expr(\"update_timestamp\"),\n  # ここで 2 を指定するだけで、開始日・終了日・フラグが自動管理される\n  stored_as_scd_type = \"2\"\n)\n```\n\n#### ✅ 過去時点の結合（Point-in-Time Join）\n分析時に当時の属性で結合する方法：\n```sql\nSELECT \n  s.order_id,\n  s.amount,\n  c.rank AS rank_at_purchase -- 購入当時のランク\nFROM sales s\nJOIN dim_customers_scd2 c\n  ON s.user_id = c.user_id\n  -- 購入日が有効期間内にある行を結合する\n  AND s.order_date >= c.valid_from\n  AND s.order_date < c.valid_to;\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **分析の正確性**: 過去の売上分析など、時点情報を重視するなら SCD Type 2 は標準装備。\n- **管理の簡素化**: `MERGE` で自作するよりも、DLT の `apply_changes` やフレームワークを活用する方が安全で保守性が高い。\n- **データ量の増大**: 変更のたびに行が増えるため、不要なカラム（頻繁に変わるが分析に使わないもの）は、このテーブルに含めない設計にする。\n",
    "4.3": "# 4.3: パフォーマンス・トラブルシューティング\n\n---\n\n### 1. 【エンジニアの定義】Professional Definition\n\n> **Data Skew (データの偏り)**:\n> 分散処理において、特定のキー（例: user_id=NULL など）にデータが集中し、一部のワーカーノードだけが過負荷になり、処理全体が停滞（Straggler）する現象。\n>\n> **Broadcast Join**:\n> 小さなテーブル（マスタ等）を全ワーカーノードに複製して配布し、ネットワーク経由の重いデータ移動（Shuffle）を回避する高速な結合手法。\n\n---\n\n### 2. 【0ベース・深掘り解説】Gap Filling\n\n#### 🔍 クエリが終わらない 3大原因\n1. **Cross Join (直積)**: JOIN条件が不適切なせいで、数千万行 × 数千万行 の全組み合わせを計算しようとしている。\n2. **Data Skew**: クラスターの 99のノードは暇なのに、1つのノードだけが 10GBのデータを処理してパンクしている。\n3. **Spill to Disk**: メモリに入り切らない中間データが遅いディスクに書き出されている。\n\nこれらを解決するには、「大きなサーバーにする」前に、**「処理するデータ量を減らす・逃がす」**設計が先決です。\n\n---\n\n### 3. 【視覚的ガイド】Visual Guide\n\n```mermaid\ngraph TD\n    subgraph Problem [\"Data Skew (偏り)\"]\n        A[Shuffle] --> B[Core 1: 50MB]\n        A --> C[Core 2: 15GB!]\n        A --> D[Core 3: 100MB]\n        C -.-> E((OOM Critical Error))\n    end\n\n    subgraph Solution [\"Optimization\"]\n        F[Broadcast small tables]\n        G[Skew Join Optimization]\n        H[Filtered Scan / ZORDER]\n    end\n```\n\n---\n\n### 4. 【技術実装】Implementation Best Practices\n\n#### ✅ Skew Join の自動補正 (AQE)\nDatabricks では、Adaptive Query Execution (AQE) を有効にすることで、データの偏りを自動検知して分割処理してくれます。\n\n```sql\n-- AQE を有効化（通常はデフォルトでON）\nSET spark.sql.adaptive.enabled = true;\n-- スキュー結合の最適化を明示的に有効化\nSET spark.sql.adaptive.skewJoin.enabled = true;\n```\n\n#### ✅ Broadcast Join のヒント\n数千万行のファクトテーブルと、数万行のマスタを結合する場合。\n```sql\nSELECT /*+ BROADCAST(m) */ \n  f.order_id, \n  m.category_name\nFROM gold.fact_sales f\nJOIN silver.category_master m ON f.category_id = m.category_id;\n```\n\n#### ✅ ファイルを物理的に並べ替える (ZORDER)\nJOINやWHERE句によく使う列を指定することで、データの読み込みスキップ効率を劇的に上げます。\n```sql\nOPTIMIZE gold.fact_sales \nZORDER BY (user_id, event_date);\n```\n\n---\n\n### 5. 【Key Takeaways】\n\n- **実行計画を読む**: Spark UI や `EXPLAIN` で「SortMergeJoin」が走っているか「BroadcastHashJoin」かを確認する。\n- **早期フィルタリング**: JOINの前に `WHERE` で可能な限り行数を絞り込む（Predicate Pushdownの活用）。\n- **物理設計の勝利**: インデックスの代わりに `ZORDER` や `PARTITION` を適切に配置することで、クエリは 10倍以上速くなる。\n"
};
