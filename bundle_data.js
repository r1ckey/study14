// THIS FILE IS AUTO-GENERATED BY build.js
const studyData = {
  "title": "SQL Mastery",
  "subtitle": "Architecture & Practical Playbook",
  "localStorageKey": "sql_mastery_progress",
  "themeColor": "#06b6d4",
  "secondaryColor": "#3b82f6",
  "axes": [
    {
      "title": "Phase 1: Design Philosophy",
      "modules": [
        {
          "id": "1.1",
          "title": "Output Driven Architecture",
          "file": "Phase1/1.1_Output_Driven_Architecture.md"
        },
        {
          "id": "1.2",
          "title": "Logical vs Physical Design",
          "file": "Phase1/1.2_Logical_vs_Physical_Design.md"
        }
      ]
    },
    {
      "title": "Phase 2: Cleansing & Prep",
      "modules": [
        {
          "id": "2.1",
          "title": "Handling Duplicates (ROW_NUMBER)",
          "file": "Phase2/2.1_Handling_Duplicates_ROW_NUMBER.md"
        },
        {
          "id": "2.2",
          "title": "Gap Filling (LEAD/LAG)",
          "file": "Phase2/2.2_Gap_Filling_LEAD_LAG.md"
        },
        {
          "id": "2.3",
          "title": "Flattening Arrays (EXPLODE)",
          "file": "Phase2/2.3_Flattening_Arrays_EXPLODE.md"
        }
      ]
    },
    {
      "title": "Phase 3: Transformation",
      "modules": [
        {
          "id": "3.1",
          "title": "Row to Column (PIVOT)",
          "file": "Phase3/3.1_Row_to_Column_PIVOT.md"
        },
        {
          "id": "3.2",
          "title": "Time Series & Windows",
          "file": "Phase3/3.2_Time_Series_and_Windows.md"
        },
        {
          "id": "3.3",
          "title": "Difference Extraction (EXCEPT)",
          "file": "Phase3/3.3_Difference_Extraction_EXCEPT.md"
        }
      ]
    },
    {
      "title": "Phase 4: ETL & Tuning",
      "modules": [
        {
          "id": "4.1",
          "title": "MERGE INTO Anatomy",
          "file": "Phase4/4.1_MERGE_INTO_Anatomy.md"
        },
        {
          "id": "4.2",
          "title": "SCD Type 2 Implementation",
          "file": "Phase4/4.2_SCD_Type_2_Implementation.md"
        },
        {
          "id": "4.3",
          "title": "Performance Troubleshooting",
          "file": "Phase4/4.3_Performance_Troubleshooting.md"
        }
      ]
    }
  ]
};
const moduleContents = {
  "1.1": "# アウトプット逆算型の分析クエリ設計手法\n\n> [!IMPORTANT]\n> **「複雑なSQLを書く時、いきなり SELECT から書き始めていませんか？」**\n> 分析用に数千行のコードを書く際、インプット（元データ）から順次結合していくアプローチは、パフォーマンス劣化やメンテナンス不能な「スパゲティコード」を生み出します。\n> プロフェッショナルなデータエンジニアリングの基本である「アウトプットから逆算する設計」を完全にマスターし、チーム全員が読める堅牢なSQLを設計しましょう。\n\n---\n\n## 1. 原則：なぜ「インプット」から考えてはいけないのか？\n\nデータエンジニアがよく陥る罠が、**「テーブルAとテーブルBをとりあえずJOINして、そこから必要な列を絞り込む」**という思考プロセスです。\n\n### 1.1 ❌ インプット駆動のアンチパターン（スパゲティコードの典型）\n```sql\n-- ❌ 思考停止でJOINを繋げた例（パフォーマンスが悪く、意図が不明）\nSELECT \n    l.user_id,\n    p.name,\n    p.age_group,\n    p.gender,\n    c.campaign_name,\n    SUM(l.view_time) as total_view_time,\n    COUNT(DISTINCT l.video_id) as unique_videos\nFROM view_logs l\nLEFT JOIN user_profiles p ON l.user_id = p.user_id\nLEFT JOIN campaigns c ON l.campaign_id = c.campaign_id\nWHERE l.date BETWEEN '2025-01-01' AND '2025-01-31'\n  AND p.status = 'active'\nGROUP BY \n    l.user_id, p.name, p.age_group, p.gender, c.campaign_name;\n```\n\n**【このコードの何が悪いのか？】**\n1. **意図が読めない**: 最終的に「何を集計したいのか」が最後まで読まないと分からない。\n2. **無駄なリソース消費**: JOINしてからGROUP BYしているため、数十億行のログデータに対して巨大なシャッフル（Shuffle）が発生し、Sparkクラスターのメモリを圧迫します。\n3. **デバッグの困難さ**: もし `total_view_time` の数値がおかしくても、どこで行が爆発したのか切り分けられません。\n\n---\n\n## 2. 解決策：アウトプット逆算思考\n\n実務におけるプロのスタイルでは、**「最終的にBIツールでどう見せたいか（Output）」**から逆算して、段階的に `WITH` 句（CTE）を構築します。\n\n### 2.1 思考プロセスの3ステップ\n1. **Output（最終結果）**: まず、最終的に出力したいカラム構成を定義する。\n2. **Process（集計・加工）**: その結果を得るために、各ファクト（履歴）テーブルを最適な粒度で事前集計（Pre-aggregation）する。\n3. **Input（抽出）**: 必要なディメンション（マスタ）テーブルを準備する。\n\n\\`\\`\\`mermaid\ngraph TD\n    classDef output fill:#0ea5e9,stroke:#fff,stroke-width:2px,color:#fff;\n    classDef process fill:#f59e0b,stroke:#fff,stroke-width:2px,color:#fff;\n    classDef input fill:#10b981,stroke:#fff,stroke-width:2px,color:#fff;\n\n    F([Output: BI用最終データ・マート]):::output\n    \n    E[Process: 事前集計済みのログとマスタの結合]:::process\n    D[Process: ユーザー単位でのログ事前集計]:::process\n    \n    A[(Input: View Logs)]:::input\n    B[(Input: User Profile)]:::input\n    C[(Input: Campaign Meta)]:::input\n\n    F -.->|逆算| E\n    E -.->|逆算| D\n    D -.->|逆算| A\n    E -.->|逆算| B\n    E -.->|逆算| C\n\n    A --> D\n    D --> E\n    B --> E\n    C --> E\n    E --> F\n\\`\\`\\`\n\n---\n\n## 3. 実践：CTEによるモジュール化設計\n\nアンチパターンで示したSQLを、**「アウトプット逆算＋事前集計」**で美しく書き直します。\n各CTE（ブロック）は「1つの明確な目的」を持ち、後続から使い回せるように設計します。\n\n### ✅ ベストプラクティスの実装\n\n```sql\nWITH \n-- ==========================================\n-- 1. Input: 対象ユーザーの抽出（要件定義ブロック）\n-- ==========================================\ntarget_users AS (\n    SELECT \n        user_id,\n        name,\n        age_group,\n        gender\n    FROM user_profiles \n    WHERE status = 'active'\n),\n\n-- ==========================================\n-- 2. Process: トランザクションの事前集計（Pre-aggregation）\n-- ※ JOINする【前】に粒度を圧縮し、シャッフルを最小化する\n-- ==========================================\naggregated_views AS (\n    SELECT \n        user_id,\n        campaign_id,\n        SUM(view_time) AS total_view_time,\n        COUNT(DISTINCT video_id) AS unique_videos\n    FROM view_logs\n    WHERE date BETWEEN '2025-01-01' AND '2025-01-31'\n    GROUP BY user_id, campaign_id\n)\n\n-- ==========================================\n-- 3. Output: 結合と最終成形（Final Assembly）\n-- ==========================================\nSELECT \n    u.user_id,\n    u.name,\n    u.age_group,\n    u.gender,\n    COALESCE(c.campaign_name, 'No Campaign') AS campaign_name,\n    v.total_view_time,\n    v.unique_videos\nFROM target_users u\n-- 既に圧縮された小さなデータ同士をJOINするため超高速\nINNER JOIN aggregated_views v \n    ON u.user_id = v.user_id\nLEFT JOIN campaigns c \n    ON v.campaign_id = c.campaign_id;\n```\n\n> [!TIP]\n> **パフォーマンス上の圧倒的メリット（Pre-aggregation）**\n> 元の巨大なログテーブル（数十億行）を他のマスタと結合（JOIN）するのではなく、先に `aggregated_views` CTEの中で `GROUP BY user_id` を行い、数百万行レベルに**圧縮してから結合**しています。これにより、Sparkのシャッフルデータ量が1/100以下になり、OOMエラーを防ぐことができます。\n\n---\n\n## 4. なぜCTE（WITH句）で分割するのか？\n\n実務においてCTEでクエリを細かく分割することは、単なる「見た目の問題」ではありません。**チーム開発におけるインフラ**です。\n\n### 4.1 デバッグの容易性\n例えば「数値がおかしい」と報告された場合、インプット駆動のクエリでは原因特定が困難です。\nしかしCTEで分割されていれば、以下のように一部をコメントアウトして即座に検証（ユニットテスト）が可能です。\n\n```sql\n-- 最終抽出部をコメントアウトし、特定のモジュールだけを出力してみる\n-- SELECT ... FROM target_users ...\n\nSELECT * FROM aggregated_views LIMIT 100;\n```\nこれにより「集計ロジックが間違っているのか」「結合キーが間違っているのか」を一瞬で切り分けることができます。\n\n### 4.2 Catalyst Optimizer（Spark）との親和性\n「CTEを使いすぎるとパフォーマンスが落ちるのでは？」と心配するエンジニアがいますが、**杞憂です**。\nDatabricksに搭載されている **Catalyst Optimizer** は記述された論理SQL（Logical Plan）を展開し、最も効率の良い物理実行計画（Physical Plan）に自動で並び替えます。\n人間目線で「最も読みやすい粒度」で細かくCTEを切るのが正解です。\n\n---\n\n## 5. 確認クイズ（理解度チェック）\n\n### Q1. 大量データのログテーブルとマスタテーブルを結合する際、JOINとGROUP BYはどちらを先に行うべきか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: GROUP BY（集計）を先に行うのがベストプラクティス**\n\n結合（JOIN）の前にデータを集計（Pre-aggregation）して行数を圧縮することで、高コストなシャッフル処理を劇的に削減できます。\n</details>\n\n### Q2. 以下のSQLの要件は「最終的に何を知りたい」のか、一目で分かりますか？分からなければどうリファクタリングすべきですか？\n\n```sql\nSELECT c.name, COUNT(o.id) \nFROM customers c JOIN orders o ON c.id = o.customer_id \nWHERE o.status = 'shipped' AND c.region = 'Tokyo'\nGROUP BY c.name;\n```\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**修正案:** CTEを使って「東京の顧客リスト」と「出荷済み注文の集計」に分け、最終SELECTで「誰ごとの出荷数か」を定義することで、初見のエンジニアにも意図が伝わるSQLになります。\n\n```sql\nWITH tokyo_customers AS (...),\nshipped_orders_by_user AS (...)\nSELECT ...\n```\n</details>\n",
  "1.2": "# Logical vs Physical Design & Testing\n\n> [!TIP]\n> **「数百行のSQLを書いて、一発で本番環境に投入していませんか？」**\n> 分析クエリもソフトウェア（コード）の一部です。自己修復機能・自己テスト機能を持たないコードは、データの変更や要件追加によって確実に壊れます。\n> 本モジュールでは、一時表（TEMP VIEW）の罠と、CTEを利用した「自己テスト機能付きクエリ」のアーキテクチャを解説します。\n\n---\n\n## 1. 概念：論理設計（人が読む）と物理設計（マシンが読む）\n\nデータ分析基盤において、SQLエンジンの進化（特にSpark Catalyst Optimizer等のモダンなCBO）により、人間が書いたコードがそのまま実行されることはなくなりました。\n\n### 1.1 論理プラン（Logical Plan）\n我々人間が書くSQLそのものです。理解しやすさ、チームでの保守性、レビューのしやすさが100%優先されます。\n無駄に見えるCTEの分割も、「人間が読むための論理的境界」として極めて重要です。\n\n### 1.2 物理実行プラン（Physical Plan）\nエンジンが背後で書き換える最適な実行手段です。「どのJOIN方式（Broadcast/SortMerge）を使うか」「どこまで先読み（Predicate Pushdown）するか」を決定します。\n\n\\`\\`\\`mermaid\ngraph LR\n    subgraph 人間が見る論理構造\n    A[CTE 1: Users] --> B[CTE 2: Filter] --> C[CTE 3: Join]\n    end\n    \n    subgraph Catalystが実行する物理構造\n    D[最適化された 1つのプロセス<br>Shuffleの最小化]\n    end\n    \n    C -.->|コンパイル&最適化| D\n\\`\\`\\`\n\n**結論:** 「パフォーマンスが落ちそうだから」という理由で、無理に1つの巨大な `SELECT` に押し込んだり、ネストを深くする（サブクエリの嵐）必要はありません。CTEでふんだんに分割し、保守性を高めるべきです。\n\n---\n\n## 2. 実務の罠：TEMP表（一時表）はオプティマイザの敵\n\nCTEで分割する代わりに、`CREATE TEMP VIEW` を使って中間データを保存するエンジニアがいます。\n\n### 2.1 ❌ TEMP表が引き起こすパフォーマンス劣化\n```sql\n-- ❌ Step 1: ユーザーを絞る\nCREATE TEMP VIEW tmp_active_users AS \nSELECT * FROM users WHERE status = 'active';\n\n-- ❌ Step 2: ログとJOINする\nSELECT a.*, l.log_time \nFROM tmp_active_users a \nJOIN logs l ON a.id = l.user_id \nWHERE l.log_time > '2025-01-01';\n```\n\n**【なぜ悪いのか？】**\n1. **限界（Boundary）の発生**: Sparkのオプティマイザは、`tmp_active_users` の作成時点では「後で日付フィルタが掛かること」を知りません。\n2. **プッシュダウン阻害**: 本来なら先にログを日付で絞り、超軽量化した状態でユーザーと結合すべきですが、TEMP表を間に挟むと「全体最適化の連鎖」が途切れます。\n\n### 2.2 ✅ CTEによる全体最適化への委譲\n```sql\nWITH active_users AS (\n    SELECT * FROM users WHERE status = 'active'\n),\njoined_logs AS (\n    SELECT a.*, l.log_time \n    FROM active_users a \n    JOIN logs l ON a.id = l.user_id \n    WHERE l.log_time > '2025-01-01'\n)\nSELECT * FROM joined_logs;\n```\n1つのスクリプトとして完結しているため、オプティマイザが「下流の WHERE 条件」を上流まで引き上げ（Predicate Pushdown）、不要なデータの読み込みを完全にスキップします。\n\n---\n\n## 3. 実践：テスト用CTEの組み込み（Test-Driven SQL）\n\n実務において「データが入っていない（NULL）」「負の売上が混ざっている」といった異常値は日常茶飯事です。\n\n### 3.1 開発時の安全弁となる `test__` ロック\n\n最終的な `SELECT` の前に、意図的に `test__` から始まるブロックを作成し、ユニットテストを組み込みます。\n\n```sql\nWITH \n  -- 1. 実データ処理ブロック\n  base_data AS (\n    SELECT * FROM transactions WHERE amount > 0\n  ),\n  aggregated AS (\n    SELECT user_id, SUM(amount) AS total FROM base_data GROUP BY 1\n  ),\n  \n  -- ★ 2. テスト用ブロック（ここが肝）\n  test__check_null AS (\n    -- user_idがNULLになっているやばいデータがないか検知\n    SELECT user_id, total, 'ERROR: userId is NULL' as error_msg\n    FROM aggregated \n    WHERE user_id IS NULL\n  ),\n  test__check_negative AS (\n    -- 集計結果がゼロやマイナスになるバグがないか検知\n    SELECT user_id, total, 'ERROR: Total is <= 0' as error_msg \n    FROM aggregated \n    WHERE total <= 0\n  )\n\n-- ==========================================\n-- 【通常運用時】\nSELECT * FROM aggregated;\n\n-- 【開発中・バグ調査時】\n-- 上記をコメントアウトし、テスト用CTEを呼び出す\n-- SELECT * FROM test__check_null;\n-- ==========================================\n```\n\n### 3.2 なぜこれが実務で強力なのか？\nデータのバグ調査を依頼された際、「どこがおかしいのか1からSQLを読み解く」のは時間の無駄です。\nこのようにテスト群がコード本体に埋め込まれていれば、他のエンジニアでも「テストブロックを順番にSELECTしてみるだけ」で不整合データの存在を瞬時に特定できます。\n\n---\n\n## 4. 確認クイズ（理解度チェック）\n\n### Q1. TEMP VIEWを使うべきシーンとは？\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: 異なる複数の言語（例えばPythonシェルからSQLの結果を使い回す場合）や、完全に別々のノートブック間でデータを一時的に共有したい場合。**\n\n同一スクリプト・クエリ内で完結する処理であれば、最適化の観点から常に CTE（WITH句） を使うべきです。\n</details>\n\n### Q2. Predicate Pushdown（条件のプッシュダウン）とは？\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: クエリの最後に書かれたWHERE句などの絞り込み条件を、システムが自動的に「データをストレージから読み込む一番最初の段階（下層）」まで押し下げてくれる最適化機能のこと。**\n\nこれにより、不必要なデータをメモリに乗せることなく、数TBのファイルから数十MBだけを読むような高速化が実現します。\n</details>\n",
  "2.1": "# Handling Duplicates (ROW_NUMBER)\n\n> [!CAUTION]\n> **「テーブル結合（JOIN）のテストをしたら、なぜか件数が1.5倍に増えていた」**\n> 実務で最もよく遭遇し、かつ最も危険なバグの温床が「一意（ユニーク）だと思っていたIDの重複」です。\n> この重複が引き起こす JOIN 爆発（Cross Join状態）を防ぐための、絶対的防御策を解説します。\n\n---\n\n## 1. ビジネスシナリオ：重複データの存在\n\n「ユーザーの住所マスタがある。複数回引っ越した人もいるが、売上データには**常にユーザーの最新の住所**だけを紐付けたい。」\n\n**【インプットデータ：住所履歴（address_history）】**\n\n| user_id | address | update_date |\n|---|---|---|\n| U100 | 東京 | 2024-01-01 |\n| U100 | 神奈川 | 2024-06-15 |\n\nこのまま売上テーブルと `user_id` だけで JOIN すると、売上の行が「過去の住所」「今の住所」の2行に分裂・複製されてしまいます。\n\n---\n\n## 2. ❌ アンチパターン (MAXとGROUP BY)\n\n初心者がやりがちなのが、`MAX(date)` などを使って一旦最新日を出し、それを自己結合（Self JOIN）で元のテーブルに戻す方法です。\n\n```sql\n-- ❌ コードが長く、二度読みが発生する遅いクエリ\nSELECT a.user_id, a.address, a.update_date \nFROM address_history a\nINNER JOIN (\n    -- 最新の日付を特定する\n    SELECT user_id, MAX(update_date) as max_date \n    FROM address_history \n    GROUP BY user_id\n) b\n-- IDと最新日の両方で結合して元のレコードを引っ張り出す\nON a.user_id = b.user_id \nAND a.update_date = b.max_date\n```\n\n**【何が危険なのか？】**\nもし、全く同じ日に2回住所変更のレコードが入っていた場合（例：オペレーターのシステム入力ミス）、`update_date = max_date` となる行が**2行**存在してしまうため、**結局重複は排除できません。**\n\n---\n\n## 3. ✅ ベストプラクティス：ROW_NUMBER関数\n\nデータの「グループ（PARTITION）」ごとに、「並び順（ORDER）」を決めて背番号を振り、常に【1番】だけを抜き出す方式です。これなら同日重複があっても強制的に1件に絞り込めます。\n\n### 3.1 CTEを利用した標準実装（汎用的）\n\n```sql\nWITH numbered_history AS (\n  SELECT \n    user_id,\n    address,\n    update_date,\n    -- ROW_NUMBER でグループごとに連番を振る\n    ROW_NUMBER() OVER (\n      PARTITION BY user_id      -- 誰ごとにグループ化するか？\n      ORDER BY update_date DESC -- 新しい順（降順）に1, 2, 3...\n    ) as row_num\n  FROM address_history\n)\n-- 必ず「1」のものだけを抽出する\nSELECT user_id, address, update_date \nFROM numbered_history \nWHERE row_num = 1;\n```\n\n### 3.2 同日重複を制御する「Tie-Breaker」のテクニック\nもし同日に2件レコードが存在した場合、システムはランダムで1番を選びます。安全性を高めるため、`ORDER BY` に**決定打となる第2のキー（IDや更新時間など）**を入れます。\n\n```sql\n    ROW_NUMBER() OVER (\n      PARTITION BY user_id\n      ORDER BY update_date DESC, system_record_id DESC -- ★第2のキー\n    ) as row_num\n```\n\n---\n\n## 4. 🥇 プロの最速技：QUALIFY句 (Databricks / Snowflake)\n\nモダンなDWH（Databricks, Snowflake, BigQueryなど）で標準化されているのが `QUALIFY` 句です。\n`WHERE` が集計前の行を絞り、`HAVING` が集計後の行を絞るなら、**`QUALIFY` はWindow関数の結果で集計後の行を絞ります。**\nCTEすら不要になります。\n\n```sql\n-- ✅ 究極にシンプルな1文\nSELECT \n  user_id, \n  address, \n  update_date \nFROM address_history\n-- Window関数の結果が「1」である行だけを残す\nQUALIFY ROW_NUMBER() OVER (\n  PARTITION BY user_id \n  ORDER BY update_date DESC\n) = 1;\n```\n\n> [!TIP]\n> **ROW_NUMBER vs RANK vs DENSE_RANK**\n> 重複を【強制的に1行だけ残す】排除目的であれば、何があっても連続する連番を振る **ROW_NUMBER()** が正解です。\n> `RANK()` を使うと、同日レコードの順位が `1, 1, 3...` となり、結局結合時に1位が2行抽出されてしまい、JOIN爆発のバグを引き起こします。\n\n---\n\n## 5. 確認クイズ（理解度チェック）\n\n### Q1. 以下の要件を満たす場合、PARTITION BY と ORDER BY には何を指定すべきか？\n「\"category\"ごとに、一番価格（price）が安い商品を抽出したい」\n\n<details>\n<summary>💡 回答を見る</summary>\n\n```sql\nROW_NUMBER() OVER (PARTITION BY category ORDER BY price ASC)\n```\nの計算結果が `1` のものを抽出します。\n</details>\n",
  "2.2": "# Gap Filling (LEAD / LAG)\n\n> [!IMPORTANT]\n> **「状態が変わった時しかログが飛んでこない」**\n> IoTセンサー、ステータス変更履歴、CRMの営業フェーズ移行ログなど、世の中の多くのデータは「差分」で保存されます。\n> これを時系列のタイムラインとして「面」で捉える（欠損値を前後の値で埋める）技術は、データエンジニアリングの最難関の一つです。\n\n---\n\n## 1. ビジネスシナリオ：状態のギャップ埋め\n\n「IoTセンサーからデバイスの稼働状態が送られてくる。通信量を抑えるため、変化があった瞬間（Activeになった、Errorになった）しかデータが来ない。」\nある毎分・毎時の状態を見たい場合、データが存在しない時間は**「直前のステータスを継続している」**とみなして穴埋めする必要があります。\n\n**【インプット：生ログ（差分）】**\n\n| Device | Time | Status |\n|---|---|---|\n| D1 | 10:00 | Active |\n| D1 | 10:05 | Error |\n| D1 | 10:15 | Active |\n\n**【目的：1分ごとのマスターと結合した時の理想結果】**\n\n| Device | Time | Status (本来の姿) |\n|---|---|---|\n| D1 | 10:00 | Active |\n| D1 | 10:01 | **Active (前を継承)** |\n| D1 | 10:02 | **Active (前を継承)** |\n| ... | ... | ... |\n| D1 | 10:05 | Error |\n\n---\n\n## 2. 解決策：LAST_VALUE + IGNORE NULLS\n\n標準的なSQLでこの「前方への穴埋め（Forward Fill）」を行おうとすると、複雑な再帰CTEや自己結合が必要になります。しかし、モダンSQLでは `IGNORE NULLS` を用いて一撃で解決可能です。\n\n### ✅ 実装パターン：前方穴埋め\n\n前提として、すべてのタイムスタンプ（毎分）の箱だけを持つカレンダーテーブルとLEFT JOINした結果、`Status`がNULLだらけになったテーブルを想像してください。\n\n```sql\nSELECT\n  device,\n  time,\n  status AS original_status,\n  \n  -- ★ 直前までの行から、一番最後の（＝最新の直近の）生きた値を取得する\n  LAST_VALUE(status) IGNORE NULLS OVER (\n    PARTITION BY device         -- デバイスごとに独立して見る\n    ORDER BY time ASC           -- 時間を昇順に並べる\n    -- ★ 窓枠：先頭から「現在の行」まで\n    ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n  ) AS forward_filled_status\n\nFROM device_status_timeline\n```\n\n**【この構文の仕組み】**\n`IGNORE NULLS` は、Window枠内の値を探す際、値がNULLの行を完全にスキップします。現在行のステータスがNULLの場合、システムは上（過去）に向かって遡り、最初に見つけた「NULLではない実体値」を採用します。\n\n---\n\n## 3. 次のイベントを予測する (LEAD / LAG)\n\nギャップ埋めと並んで重要なのが、「このイベントが起きてから、次のイベントが起きるまでの**滞在時間（リードタイム）**を知りたい」という要件です。\n\n### 3.1 ❌ 自己結合を使う（アンチパターン）\n「今の行」の次のデータを探すために、`t1.time < t2.time` などを条件に自己結合すると、O(N^2)の計算量となり一瞬でサーバーが落ちます。\n\n### 3.2 ✅ LEAD による次行へのアクセス\n`LEAD()` 関数は、物理的に「次の行」の値を瞬時に取得します。\n\n```sql\nSELECT\n  user_id,\n  event_time,\n  event_type,\n  \n  -- 1. 次に起きたイベントの時間を持ってくる\n  LEAD(event_time) OVER (\n      PARTITION BY user_id \n      ORDER BY event_time ASC\n  ) as next_event_time,\n  \n  -- 2. DATEDIFFで時間差分を計算（例：秒単位）\n  datediff(\n    second, \n    event_time, \n    LEAD(event_time) OVER (PARTITION BY user_id ORDER BY event_time ASC)\n  ) as duration_seconds\n\nFROM user_events\n```\n\n> [!TIP]\n> `LEAD` の逆で「前の行」の値を取得するのは `LAG` 関数です。\n> `LEAD(column, 1)` は1個次、`LEAD(column, 3)` なら3個次の値を取るなど、ステップ数の指定も可能です。さらに、次の行が存在しない（最後の行の）場合に返すデフォルト値も `LEAD(column, 1, 'End')` のように設定できます。\n\n---\n\n## 4. 確認クイズ（理解度チェック）\n\n### Q1. LEADとLAGの違いは？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解:** `ORDER BY` で指定されたソート順に対して、\n- **LAG**: 前の行（過去）の値を取得する。\n- **LEAD**: 後の行（未来・次）の値を取得する。\n</details>\n\n### Q2. IGNORE NULLS は何をしてくれる機能ですか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解:** ウィンドウ関数（LAST_VALUEなど）が評価対象の範囲（窓）を探す際、値がNULLであるレコードを「そもそも存在しないもの」として無視し、NULL以外の有効な値を確実に拾い上げる手助けをしてくれます。前回の状態を引き継ぐ Forward Fill の必須機能です。\n</details>\n",
  "2.3": "# Flattening Arrays (EXPLODE)\n\n> [!TIP]\n> **「カラムの中にカンマ区切りで入っているリストを開きたい」**\n> 昨今のデータ基盤（特にData LakeやNoSQL由来のデータ）において、データは綺麗な表形式（RDB）で連携されるとは限りません。\n> JSONの配列（Array）を縦の行に展開する（Flattening）強力な技術をマスターしましょう。\n\n---\n\n## 1. ビジネスシナリオ：入れ子になったデータの展開\n\n「ECサイトの注文システムから送られてくる購入データで、複数の購入アイテムが1つの配列（Array型）として1カラムに入ってしまっている。\nこれを商品ごとの売上ランキングとして集計したいので、**1行1アイテムになるよう縦に分解**したい。」\n\n**【インプットデータ：注文テーブル（orders）】**\n\n| order_id | user_id | items_array |\n|---|---|---|\n| O-001 | U-A | [\"Apple\", \"Banana\"] |\n| O-002 | U-B | [\"Orange\"] |\n| O-003 | U-C | [] (空配列) |\n\nこれを、以下のようにフラット化（展開）しなければBIツールで表現できません。\n\n**【理想の出力】**\n\n| order_id | user_id | item_name |\n|---|---|---|\n| O-001 | U-A | Apple |\n| O-001 | U-A | Banana |\n| O-002 | U-B | Orange |\n\n---\n\n## 2. 解決策：EXPLODE による縦への爆破\n\n`EXPLODE` は、配列の中身を一つ一つ「爆発」させて、要素数と同じ数だけ行を増殖（複製）させてくれる関数です。\n\n### 2.1 ✅ 基本構文（LATERAL VIEW）\nHiveなど、古くから使われている確実な記法です。\n\n```sql\nSELECT\n  order_id,\n  user_id,\n  item_name\nFROM orders\n-- LATERAL VIEW EXPLODE を使用して、配列カラムを新しい仮想的なテーブルに変換し結合\nLATERAL VIEW EXPLODE(items_array) AS item_name;\n```\n\n### 2.2 ✅ モダン構文（Databricks等）\nSpark 3.x以降や最新のSQLエンジンでは、`SELECT` の中に直接関数のように記述できるようになり、圧倒的に短く書けます。\n\n```sql\nSELECT\n  order_id,\n  user_id,\n  -- カラム内で直接爆破して別名をつける\n  EXPLODE(items_array) AS item_name\nFROM orders;\n```\n\n\\`\\`\\`mermaid\ngraph LR\n    A[Order O-001: <br>Array(Apple, Banana)] --> B((EXPLODE!))\n    B --> C[行1: O-001, Apple]\n    B --> D[行2: O-001, Banana]\n\\`\\`\\`\n\n---\n\n## 3. 実務の壁：EXPLODE の罠\n\n`EXPLODE` は非常に強力ですが、実務で使うと必ずぶつかる2つの壁があります。\n\n### 3.1 罠①：空配列のレコードが消滅する\n`EXPLODE( [空配列] )` を実行すると、その親行（上記の O-003 の注文レコード）自体が**結果から完全に消滅します**（INNER JOINのような扱いになるため）。\n「買った商品は無いけど、注文ログとしては残したい」場合に集計が狂います。\n\n**【解決策】：EXPLODE_OUTER**\n`LEFT JOIN` の性質を持つ `EXPLODE_OUTER` を使います。\n\n```sql\nSELECT order_id, EXPLODE_OUTER(items_array) AS item_name FROM orders;\n-- 結果： O-003 | NULL  として行は担保される\n```\n\n### 3.2 罠②：順番が分からなくなる（元は何番目だった？）\n例えば、「一番目にカートに入れた商品と、最後にカートに入れた商品を分析したい」場合、単純な `EXPLODE` では配列の元のインデックス（インデント順位）が失われます。\n\n**【解決策】：POSEXPLODE（Position Explode）**\n配列の「値」とともに、「インデックス番号（0始まり）」も同時に返してくれます。\n\n```sql\nSELECT\n  order_id,\n  pos AS array_index, -- 0, 1, 2...\n  item_name\nFROM orders\nLATERAL VIEW POSEXPLODE(items_array) AS pos, item_name;\n\n-- 新構文の場合（結果が配列などで返る）\n-- SELECT order_id, POSEXPLODE(items_array) FROM orders;\n```\n\n---\n\n## 4. 確認クイズ（理解度チェック）\n\n### Q1. カンマ区切りのただの文字列 `\"Apple,Banana\"` が入ったカラムを EXPLODE するには？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n直接文字列を変数に入れることはできません。まず `SPLIT` 関数を使って文字列を強引に Array（配列）に変換してから爆破します。\n\n```sql\nSELECT EXPLODE(SPLIT(item_string, ',')) as item_name FROM orders;\n```\n</details>\n\n### Q2. ログ内の1行の中で2つの異なる配列（例: views_array と clicks_array）を同時に EXPLODE した場合はどうなる？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**CROSS JOIN爆発が起きます。**\nviewsが3要素、clicksが3要素ある行の場合、1回の `SELECT` 区句内で2つ `EXPLODE` すると、3 × 3 = 9行に増殖してしまい、全く意味のない組み合わせが生成されます。\n複数展開が必要な場合は、CTEを分けて段階的に処理するか、配列同士をZIP（結合）してから展開する必要があります。\n</details>\n",
  "3.1": "# Row to Column (PIVOT)\n\n> [!IMPORTANT]\n> **「テーブルの特定の文字列を、カラム名（列名）に昇格させたい」**\n> システムはデータを保存する際、拡張しやすいように「縦待ち（正規化）」で保存します。しかし、BIツールや機械学習モデルは「横持ち」の特徴量テーブルを要求します。\n> この縦から横へのトランスフォームを自在に操れるかどうかが、データエンジニアの腕の見せ所です。\n\n---\n\n## 1. ビジネスシナリオ：アンケートデータの横持ち化\n\n「ユーザーからのアンケート回答がデータベースに蓄積されている。質問ごとの回答が縦に入っているが、これを機械学習の特徴量（1ユーザー1行）に変換したい。」\n\n**【インプット（縦持ち：EAVモデル）】**\n\n| user_id | question_id | answer |\n|---|---|---|\n| U1 | Q1 | Yes |\n| U1 | Q2 | No |\n| U2 | Q1 | No |\n\n**【理想のアウトプット（横持ちの特徴量）】**\n\n| user_id | answer_q1 | answer_q2 | answer_q3 |\n|---|---|---|---|\n| U1 | Yes | No | NULL |\n| U2 | No | NULL | NULL |\n\n---\n\n## 2. ❌ アンチパターン：無限の自己結合 (Self-Join)\n\nSQLの基礎機能だけでこれを作ろうとすると、質問の数だけ `LEFT JOIN` を繰り返すハメになります。\n\n```sql\n-- ❌ コードが肥大化し、結合するたびにパフォーマンスが極端に悪化する\nSELECT \n  u.user_id,\n  q1.answer AS answer_q1,\n  q2.answer AS answer_q2\nFROM (SELECT DISTINCT user_id FROM survey_results) u\nLEFT JOIN survey_results q1 ON u.user_id = q1.user_id AND q1.question_id = 'Q1'\nLEFT JOIN survey_results q2 ON u.user_id = q2.user_id AND q2.question_id = 'Q2'\n-- 質問が100個あったら終わる...\n```\n\n---\n\n## 3. ✅ 解決策1：CASE WHEN + GROUP BY（古典的だが最強）\n\n全てのRDBMSで確実に動き、パフォーマンスも安定している伝統的かつ実践的な手法です。\n\n```sql\n-- ✅ 実務で最も愛される確実な手法\nSELECT \n  user_id,\n  -- Q1の答えだけを取り出し、それ以外は無視して統合する\n  MAX(CASE WHEN question_id = 'Q1' THEN answer END) AS answer_q1,\n  MAX(CASE WHEN question_id = 'Q2' THEN answer END) AS answer_q2,\n  MAX(CASE WHEN question_id = 'Q3' THEN answer END) AS answer_q3\nFROM survey_results\nGROUP BY user_id;\n```\n\n**【なぜ `MAX` を被せるのか？】**\n`GROUP BY user_id` をした瞬間、DBエンジンは「U1」の行を1行にまとめようとします。文字列であっても、「Q1のYesか、それ以外のNULLか」を比較して生き残らせるために、ダミーの集計関数として `MAX`（または `MIN`）を通すのがセオリーです。\n\n---\n\n## 4. ✅ 解決策2：PIVOT 構文（モダンな書き方）\n\nモダンなSQLエンジンでは `PIVOT` キーワードによる専用構文が用意されています。\n\n```sql\nSELECT *\nFROM (\n  -- ① PIVOTさせたい元のテーブル定義\n  SELECT user_id, question_id, answer \n  FROM survey_results\n)\nPIVOT (\n  -- ② 交差点（セル）に入れたい値の【集計関数】\n  MAX(answer) \n  \n  -- ③ 【元の値】を【新しいカラム名】に変換するマッピング\n  FOR question_id IN (\n    'Q1' AS answer_q1, \n    'Q2' AS answer_q2, \n    'Q3' AS answer_q3\n  )\n)\n```\n\n\\`\\`\\`mermaid\ntable\n    User_ID --> PIVOT_Engine\n    Question --> PIVOT_Engine\n    Answer --> PIVOT_Engine\n    \n    PIVOT_Engine --> Col_U1[U1 | Yes | No]\n    PIVOT_Engine --> Col_U2[U2 | No | NULL]\n\\`\\`\\`\n\n---\n\n## 5. 確認クイズ（理解度チェック）\n\n### Q1. PIVOTを使う際、集計関数（MAXやSUM）を書かずにそのまま値を入れることは可能ですか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: 不可能です。**\n\nPIVOTの裏側では必ず `GROUP BY` に相当する集計が走ります。なぜなら「U1がQ1を2回回答していた場合」データベースはどうしていいか分からないためです。文字なら `MAX`、数値なら `SUM` や `AVG` など、意図を示す関数が必須です。\n</details>\n\n### Q2. 実行するまで対象の質問の数や種類（Q1, Q2, Q4...）が分からない場合、動的にカラム名を作る Dynamic PIVOT は通常のSQLで書けますか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: 直接は書けません。**\n\n標準SQLはコンパイル時に出力カラムのスキーマが確定している必要があります。動的にカラム数が増えるPIVOTを行いたい場合は、Python（PySparkの `df.pivot()`）を使うか、SQL生成のストアドプロシージャを組む必要があります。\n</details>\n",
  "3.2": "# Time Series & Windows\n\n> [!TIP]\n> **「直近30日の移動平均を、日付別に出してほしい」**\n> このオーダーに対し、日付の自己結合（Cross Join + 範囲WHERE）を書いてしまうとあっという間にシステムがメモリ不足でダウンします。\n> データに触る回数を最小限に抑え、劇的にパフォーマンスを改善する「スライディングウィンドウ」の極意を解説します。\n\n---\n\n## 1. ビジネスシナリオ：トレンドの可視化\n\n「日々の小売店の売上（daily_sales）が曜日によって乱高下するので、トレンドが見えない。過去7日間の移動平均（7-Day Moving Average）を算出してKPIダッシュボードに出したい。」\n\n---\n\n## 2. ❌ アンチパターン：非効率な自己結合\n\n昔ながらのSQLでこれを書こうとすると、「集計基準日」に対して「過去7日分のデータ」を無理やり掛け合わせる形になります。\n\n```sql\n-- ❌ データ件数が数万行を超えたあたりから終わらなくなるクエリ\nSELECT \n  t1.date,\n  t1.daily_sales,\n  AVG(t2.daily_sales) AS rolling_7d_avg\nFROM retail_sales t1\n-- 各々の日に、自分を含めた過去6日分を強引に結びつける\nINNER JOIN retail_sales t2 \n  ON t2.date BETWEEN DATE_SUB(t1.date, 6) AND t1.date\nGROUP BY t1.date, t1.daily_sales\nORDER BY t1.date;\n```\n1年のデータ（365日）ごとに7回JOINされ、行数が爆発します。最悪のO(N^2)処理の一種です。\n\n---\n\n## 3. ✅ ベストプラクティス：ROWS BETWEEN\n\n`OVER` 句の中に `ROWS BETWEEN` を指定し、「計算対象とする窓（スライディングウィンドウ）」を定義します。これにより、エンジンはデータを日付順に1回だけスキャンしながら、並行して平均を足し引き（ローリング）していく極めて効率的な計算（O(N)）を行います。\n\n```sql\nSELECT\n  date,\n  daily_sales,\n  \n  -- ★ 直前6行 ＋ 当日行 ＝ 計7行 の平均をとる\n  AVG(daily_sales) OVER (\n    ORDER BY date ASC\n    ROWS BETWEEN 6 PRECEDING AND CURRENT ROW\n  ) AS rolling_7d_avg\n  \nFROM\n  retail_sales\nORDER BY date ASC;\n```\n\n---\n\n## 4. ⚠️ 罠：カレンダーの抜け漏れ (ROWS vs RANGE)\n\nもし「店が定休日で、売上レコードが存在しない日」があったらどうなるでしょう？\n\n**【ROWS の場合】**\n`ROWS BETWEEN 6 PRECEDING` は「純粋に上の行え数えて6行」です。\n定休日があった場合、実際は「カレンダー上で10日前」のデータを拾ってしまい、巨大なスパイクがあった場合に平均を歪めます。\n\n**【RANGE の場合】**\n行数ではなく、「数値や日付の値」そのものを基準に窓を作ります。日付型の RANGE 計算をフルサポートしている環境（Databricks / Spark SQLなど）であれば、こちらが完全な正解です。\n\n```sql\nSELECT\n  date,\n  daily_sales,\n  \n  -- ★ 行数に依存せず、絶対的な「日付ベースで過去6日前まで」を見る\n  AVG(daily_sales) OVER (\n    ORDER BY CAST(date AS TIMESTAMP) \n    RANGE BETWEEN INTERVAL 6 DAYS PRECEDING AND CURRENT ROW\n  ) AS exact_7d_avg\nFROM\n  retail_sales;\n```\n\n---\n\n## 5. 確認クイズ（理解度チェック）\n\n### Q1. 「累積売上高（YTD: Year to Date）」を出す場合、Windowの開始地点はどう記載するか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n「その年の最初の行（どこまでも前）」から「現在」までなので、`UNBOUNDED PRECEDING` を使用します。\n\n```sql\nSUM(daily_sales) OVER (\n  PARTITION BY year(date)\n  ORDER BY date ASC\n  ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n)\n```\n</details>\n\n### Q2. 過去3日間の平均を出すため `ROWS BETWEEN 2 PRECEDING AND CURRENT ROW` を使いました。最初の2日分（対象が3日に満たない日）の計算はどう処理されますか？エラーになりますか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: エラーにならず、存在する行だけで平均が計算されます。**\n\n1日目はその日だけの平均、2日目は2日分での平均になります。\nこれを防いで「3日揃っていない時はNULLにしたい」場合は、`CASE WHEN COUNT(*) OVER (...) = 3 THEN ... END` のように事前にカウントチェックを噛ませるテクニックが必要です。\n</details>\n",
  "3.3": "# Difference Extraction (EXCEPT / ANTI JOIN)\n\n> [!CAUTION]\n> **「テーブルAにあって、テーブルBにないデータを探したい」**\n> まさか `NOT IN (SELECT ...)` を使っていませんか？数万件以上のデータでこれをやると、クエリが永遠に返ってこなくなります。\n> 大規模分散処理において最も安全で最速の『差分抽出設計』をマスターしましょう。\n\n---\n\n## 1. ビジネスシナリオ：新規・退会・差分の特定\n\n「毎日数百万件のユーザーマスタが外部DBから送られてくる（全量スナップショット）。前日のリストと本日のリストを比較して、『新しく追加されたID（新規入会）』だけにウェルカムメールを送りたい。」\n\n**【今月の全ユーザー (A)】**：100万人\n**【先月の全ユーザー (B)】**：98万人\n\n---\n\n## 2. ❌ アンチパターン：NOT IN サブクエリ\n\nもっとも「人間の言葉（自然言語）」に近いですが、SQLエンジンにとっては悪夢のような重さを誇る書き方です。\n\n```sql\n-- ❌ テーブルAの全ての行に対し、テーブルBの中にないかフルスキャンを行おうとする場合がある\nSELECT user_id, email \nFROM users_this_month\nWHERE user_id NOT IN (\n    SELECT user_id FROM users_last_month\n);\n```\n\n**【致命的な問題】**\nもし `users_last_month` 側の `user_id` に1件でも `NULL` が含まれていた場合、`NOT IN` の論理評価は `UNKNOWN(NULL)` となり、**「結果が0件」になってしまう深刻なバグ**を抱えています。\n\n---\n\n## 3. ✅ 解決策1：EXCEPT (カラム全体での完全一致差分)\n\n2つのクエリの結果同士を「集合的に引き算」する構文です。\n\n```sql\n-- 今月の全ユーザー集合 (A)\nSELECT user_id, email FROM users_this_month\nEXCEPT\n-- 先月の全ユーザー集合 (B)\nSELECT user_id, email FROM users_last_month\n\n-- 結果 => (A - B) の差分。今月だけの新規ユーザーが出力される\n```\n\n**【メリットとデメリット】**\n- **メリット**: まったく同じ構造のテーブル同士（スナップショット等）の比較に超強力。\n- **デメリット**: SELECTした「すべての列」を比較して差分を出すため、「ID」だけをキーにして特定の状態やメールアドレスを引っ張るような複雑な条件式には不向きです。\n\n---\n\n## 4. ✅ 解決策2：LEFT ANTI JOIN (実務上の最強ソリューション)\n\n最も実務的、かつオプティマイザが最高の効率（Hash Joinなど）を出せる結合方式です。\n「左側のテーブルにはあるが、右側には**存在しない(マッチしなかった)**データのみ」を直接出力します。\n\n```sql\nSELECT \n  t.user_id, \n  t.email \nFROM \n  users_this_month t\n-- ★ ANTI JOIN ＝ 一致しない行だけを残す特殊なJOIN\nLEFT ANTI JOIN \n  users_last_month l \n  ON t.user_id = l.user_id;\n\n-- ※ 従来の LEFT JOIN + IS NULL よりも意図が明確で高速です\n```\n\n\\`\\`\\`mermaid\ngraph LR\n    subgraph This_Month (左テーブル)\n    A(ID:1)\n    B(ID:2)\n    C(ID:3)\n    end\n    \n    subgraph Last_Month (右テーブル)\n    D(ID:1)\n    E(ID:2)\n    end\n    \n    A -.->|マッチ・消滅| D\n    B -.->|マッチ・消滅| E\n    C == 右に無い! ==> F((結果として出力<br>ID3))\n    \n    style F fill:#06b6d4,stroke:#fff,color:#fff\n\\`\\`\\`\n\n---\n\n## 5. 補足：既存の変更点を探したい場合 (INNER JOIN)\n\nもし「IDは同じなんだけど、Emailアドレスが変わった人」を探したい場合はどうするか？\nその場合は素直に `INNER JOIN` をして中身を比較します。\n\n```sql\nSELECT \n  t.user_id, \n  l.email as old_email,\n  t.email as new_email\nFROM users_this_month t\nINNER JOIN users_last_month l \n  ON t.user_id = l.user_id\n-- 旧データと新データで値が違う人\nWHERE t.email != l.email;\n```\n\n---\n\n## 6. 確認クイズ（理解度チェック）\n\n### Q1. NOT IN と NOT EXISTS では、どちらが安全でパフォーマンスが良いでしょうか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: `NOT EXISTS` です。**\n\n`NOT IN` はサブクエリ内に一つでも NULL が含まれると一切機能しなくなる致命的弱点がありますが、`NOT EXISTS` は安全です。\nただし、現在のSparkエンジン等の基盤では `NOT EXISTS` と書いた瞬間に、内部で自動的に `LEFT ANTI JOIN` の物理プランに翻訳して実行してくれます。\n</details>\n\n### Q2. 「今月の退会者」を知るには上記の LEFT ANTI JOIN をどう書き換えればよいでしょうか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: 左と右のテーブル指定を逆にします。**\n\n先月のリスト(Left)にあって、今月のリスト(Right)に無い人を探せば良いので：\n```sql\nSELECT l.user_id \nFROM users_last_month l \nLEFT ANTI JOIN users_this_month t ON l.user_id = t.user_id\n```\nとなります。\n</details>\n",
  "4.1": "# MERGE INTO 解体新書：堅牢なデータパイプラインの最強の切り札\n\n> [!CAUTION]\n> **「バッチ処理の途中でエラーが起きた」**\n> この時、すでにINSERTされてしまったデータを手動で消して再実行していませんか？\n> \\`MERGE INTO\\` をマスターすれば、冪等性（何度実行しても必ず同じ結果になる）が担保され、エラーに強い堅牢なパイプラインが完成します。\n\n---\n\n## 1. そもそも MERGE INTO とは何か？\n\nデータ連携処理において、**「ターゲットにデータが既に存在していれば更新（UPDATE）、存在していなければ新規追加（INSERT）」**というロジックは実務で1日100回登場します。これを **Upsert（アップサート）** と呼びます。\n\n### 1.1 ❌ 従来の危険なアプローチ\n昔のデータベースや一部の処理では、これを2段構えで書いていました。\n\n```sql\n-- ❌ Step 1: まずUPDATEを試みる\nUPDATE target_table t\nSET t.value = s.value\nFROM source_table s WHERE t.id = s.id;\n\n-- ❌ Step 2: 次に存在しないものだけINSERTする\nINSERT INTO target_table (id, value)\nSELECT id, value FROM source_table s\nWHERE NOT EXISTS (SELECT 1 FROM target_table t WHERE t.id = s.id);\n```\n\n**【何が危険なのか？】**\nStep 1 と Step 2 の間でサーバーがクラッシュした場合、データが不整合を起こします。また、コードが二重になりメンテナンスビリティも最悪です。\n\n### 1.2 ✅ MERGE INTO による解決\n\\`MERGE INTO\\`を使えば、上記の処理を**「1つのACIDトランザクション」**として完結させることができます。落ちてもロールバックされるため安全です。\n\n---\n\n## 2. MERGE INTO 構文の解剖（実務テンプレート）\n\nDatabricks等における実務利用を想定し、最も堅牢な `MERGE INTO` の書き方を解説します。\n\n\\`\\`\\`mermaid\ngraph TD\n    classDef target fill:#0f172a,stroke:#3b82f6,stroke-width:2px,color:#fff;\n    classDef source fill:#0f172a,stroke:#10b981,stroke-width:2px,color:#fff;\n    classDef match fill:#f59e0b,stroke:#fff,stroke-width:2px,color:#fff;\n\n    A[Target Table<br>ターゲット]:::target\n    B[Source Data<br>新しい差分データ]:::source\n\n    A & B --> C{ON 句<br>IDの一致判定}\n\n    C -->|一致した (WHEN MATCHED)| D[UPDATE / 必要な行だけ更新]:::match\n    C -->|一致しない (WHEN NOT MATCHED)| E[INSERT / 新規レコード追加]:::match\n\\`\\`\\`\n\n```sql\n-- ① 【どこを】書き換えるか（ターゲット宣言）\nMERGE INTO silver.customer_master AS target\n\n-- ② 【どのデータを】持ち込むか（ソースのインライン定義）\n-- ※ CTEを外で作るのではなく、USING句の中に直接書く事も可能\nUSING (\n  SELECT \n    id, \n    name, \n    status,\n    ROW_NUMBER() OVER(PARTITION BY id ORDER BY updated_at DESC) as rn\n  FROM bronze.daily_customer_extract\n  WHERE is_valid = true\n) AS source\n\n-- ③ 【何をもって】同一と判定するか（キー照合）\nON target.id = source.id \n   AND source.rn = 1  -- 注意：ソース側でIDが重複しているとエラーになるため1行に絞る\n\n-- ④ 一致した場合の処理（UPDATE）\nWHEN MATCHED \n  -- ★プロの技: 不必要な更新（無駄な書き込みIO）を防ぐため、値が変化した時だけ更新する\n  AND (target.name != source.name OR target.status != source.status) \nTHEN \n  UPDATE SET \n    target.name = source.name,\n    target.status = source.status,\n    target.updated_at = current_timestamp()\n\n-- ⑤ 一致しなかった場合の処理（INSERT）\nWHEN NOT MATCHED THEN\n  INSERT (\n    id, \n    name, \n    status, \n    created_at, \n    updated_at\n  )\n  VALUES (\n    source.id, \n    source.name, \n    source.status, \n    current_timestamp(), \n    current_timestamp()\n  );\n```\n\n---\n\n## 3. 実務で「絶対に」やってはいけないアンチパターン\n\n### 3.1 ❌ ソース側のユニーク制約違反\n`MERGE` を実行した際、Target（既存）の1行に対して、Source（新）の**複数行**が一致してしまうと、エンジンは「どちらの値で更新すればいいか分からない」ため、深刻なエラーを吐いて停止します。\n\n- **解決策**: `USING` 句の中で `ROW_NUMBER() = 1` 等を用いて、必ずSource側が**ユニーク（一意）**になるように前処理を行ってください。（フェーズ2参照）\n\n### 3.2 ❌ 全カラムの盲目的なUPDATE\n`WHEN MATCHED THEN UPDATE SET *` と書くと、値が変わっていなくても全行が更新対象となり、膨大な無駄な書き込み（トランザクションログの肥大化）が発生します。\n- **解決策**: 先述のテンプレートのように `AND (target.val != source.val)` のような安全弁を必ず入れましょう。\n\n---\n\n## 4. 応用：論理削除（Soft Delete）との連動\n\n「データが送られてこなくなった = 削除されたとみなす」という実務要件のケースです。\n`WHEN NOT MATCHED BY SOURCE` という拡張構文（Databricks独自等）を利用します。\n\n```sql\nMERGE INTO silver.customer_master AS target\nUSING active_customers_from_api AS source\nON target.id = source.id\n\nWHEN MATCHED THEN \n  UPDATE SET target.name = source.name\n\n-- 「ターゲット(既存)には存在するが、ソース(最新API)には存在しなかった」場合の処理\nWHEN NOT MATCHED BY SOURCE AND target.status != 'deleted' THEN\n  UPDATE SET \n    target.status = 'deleted',\n    target.deleted_at = current_timestamp()\n```\n\n---\n\n## 5. 確認クイズ（理解度チェック）\n\n### Q1. Source側のデータに全く同じIDが2行（鈴木さんと佐藤さん）含まれていて、Target側にそのIDが存在する場合、MERGE文はどうなるか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: ランタイムエラー（実行時エラー）が発生する。**\n\nどちらの値（鈴木か佐藤か）で更新すれば良いかエンジンが判断できないため、`MERGE` は失敗します。これを防ぐため、事前に一意化（重複排除）を確実に行う必要があります。\n</details>\n\n### Q2. 冪等性（べきとうせい）とはどういう意味ですか？MERGEがなぜそれを担保できるのですか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: 「何度同じ処理を実行しても、結果が同じ状態になること」**\n\n単なる `INSERT` の場合、2回実行するとデータが2重に（ダブって）入ってしまいます。しかし `MERGE` は既存キーを確認するため、何度実行しても「最新の状態が正しく反映されるだけ」となり、リトライ処理が極めて安全になります。\n</details>\n",
  "4.2": "# SCD Type 2 Implementation\n\n> [!IMPORTANT]\n> **「なぜ過去の売上実績が勝手に変わっているんだ！？」**\n> 分析基盤において「ユーザーの属性情報を単純上書き（UPDATE）」することは絶対のタブーです。\n> 顧客の会員ランクは「通常」から「プレミアム」に変動します。売上分析は「当時のランク」で行わなければなりません。\n> この履歴管理の課題を解決する、DWH設計の真髄『SCD (Slowly Changing Dimension) Type 2』を解説します。\n\n---\n\n## 1. 概念：SCD (ゆっくり変化する次元)\n\nマスタデータ（次元テーブルの属性）の変更をどう扱うかには、世界共通の型（Type）が存在します。\n\n### Type 1: 単純な上書き（Overwrite）\n歴史的経緯はどうでもよく、現在の値だけを知りたい場合に使います。\n住所変更など。過去の出荷データなども最新住所で集計されてしまうデメリットがあります。\n\n### Type 2: 新規行の追加による履歴管理（Versioning）\nデータを上書きせず、「有効開始日（valid_from）」と「有効終了日（valid_to）」、さらに「最新フラグ（is_current）」を持たせることで履歴の断面を残します。\n\n**【顧客次元（dim_customer）の例】**\n\n| ID | ランク | valid_from | valid_to | is_current |\n|---|---|---|---|---|\n| 01 | 通常 | 2022-01-01 | 2023-06-01 | FALSE |\n| 01 | プレミアム | 2023-06-01 | 9999-12-31 | **TRUE** |\n\n---\n\n## 2. ❌ 自作SQLによる泥臭い実装（MERGEの応用）\n\nSCD Type 2の実装は、「現在の有効な行を古くする（無効化 UPDATE）」しつつ、「新しい行を追加（INSERT）」するという2つのアクションが必要になります。純粋な `MERGE INTO` だけで書こうとすると以下のような黒魔術になります。\n\n```sql\n-- ❌ エラーを踏みやすく、可読性も低いアンチパターン\n-- (仕組みを知るための概念コードであり、実運用では避けるべき)\nMERGE INTO dim_customer AS target\nUSING (\n  -- ① 値が変わった更新用のフラグ行\n  SELECT id, rank, 'UPDATE' as act FROM cdc_changes\n  UNION ALL\n  -- ② 新しい値の追加用のフラグ行\n  SELECT id, rank, 'INSERT' as act FROM cdc_changes\n) AS source\nON target.id = source.id AND target.is_current = true\n\n-- 更新用データと一致した場合、既存レコードを「無効化」\nWHEN MATCHED AND source.act = 'UPDATE' AND target.rank != source.rank THEN\n  UPDATE SET \n    is_current = false,\n    valid_to = current_timestamp()\n\n-- 追加用データの場合は、新規レコードとして書き込み\nWHEN NOT MATCHED THEN\n  INSERT (id, rank, valid_from, valid_to, is_current)\n  VALUES (source.id, source.rank, current_timestamp(), '9999-12-31', true)\n```\n\nこの実装はトランザクション順序の考慮や、同日に2回変更が来た場合の考慮漏れ（CDCの順序性担保）が起きやすく、データエンジニアリング上の大きな技術的負債となります。\n\n---\n\n## 3. ✅ 現代的な解決策：フレームワークへの委譲（DLT APPLY CHANGES）\n\nデータパイプライン構築における最大のベストプラクティスは、「車輪の再発明をしない」ことです。\nDatabricksでは、**Delta Live Tables (DLT)** という機能の `APPLY CHANGES INTO` 命令を使うと、この複雑なSCD Type 2の履歴管理を**パラメータだけで完全自動化**してくれます。\n\n### 3.1 DLT (Python) によるエレガントな実装\n\n```python\n# 魔法のようにType 2の履歴管理テーブルが完成する\ndlt.apply_changes(\n  target = \"dim_customer_history\",\n  source = \"customer_cdc_stream\",\n  keys = [\"id\"],\n  # 変更イベントが発生した時間のカラム。処理の前後が入れ替わって到着しても保証される\n  sequence_by = dlt.expr(\"update_timestamp\"),\n  \n  # Type 2を明示的に指定（デフォルトはType 1）\n  stored_as_scd_type = \"2\",\n  \n  # 削除フラグを持つカラムがあればそれも渡す\n  apply_as_deletes = dlt.expr(\"is_deleted = true\")\n)\n```\n\n**【裏側で何が起きているか？】**\n自動的に `__START_AT` や `__END_AT` というシステムカラムが追加され、裏側のSparkエンジンが最も安全でパフォーマンスの高い方法でUpsertや履歴保存の `MERGE` を実行し続けてくれます。\n\n---\n\n## 4. 確認クイズ（理解度チェック）\n\n### Q1. ファクト（売上実績など）側から Type 2のディメンション（顧客マスタなど）をJOINする際、結合条件にはどのような工夫が必要ですか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: IDの一致だけでなく、売上発生日が「マスタの有効期間内」に収まっているかを条件に入れます。**\n\n```sql\nSELECT f.sales_amount, d.rank\nFROM fact_sales f\nINNER JOIN dim_customer d\n  ON f.customer_id = d.id\n  -- ファクトの日付が、その当時のマスタの有効期間内であること\n  AND f.sales_date >= d.valid_from \n  AND f.sales_date < d.valid_to\n```\nこれによって「購入した当時の会員ランク」を正確に引っ張ることができます。\n</details>\n\n### Q2. SCD Type 3 とは何ですか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: 横持ちで履歴を一つだけ持つタイプです。**\n`old_rank` と `current_rank` のように、前の状態だけをカラムとして横に持ちます。直近一回の変化だけ見えればよい安価な管理手段ですが、最近の大容量DWH環境ではType 2に統一されることがほとんどです。\n</details>\n",
  "4.3": "# Performance Troubleshooting\n\n> [!CAUTION]\n> **「昨日まで終わっていたクエリが、今日はいつまで経っても終わらない」**\n> **「SparkクラスターがOOM（OutOfMemory）で頻繁にクラッシュする」**\n> エンジニアなら誰もが経験するこの現象に対し、盲目的に「サーバーのスペック（メモリ）を上げる」という対処をするのはプロ失格です。\n> 実行計画（Execution Plan）から犯人を立証する、トラブルシューティングの極意を解説します。\n\n---\n\n## 1. 致命的な原因①：Cross Join (直積の嵐)\n\nトラブルの8割はこれと言っても過言ではありません。\n\n`JOIN` の `ON` 条件が抜けていたり、無意味な評価（`ON 1=1`）になっていたりすると、システムは表Aの全行と表Bの全行を「総当たり」で掛け合わせる **Cartesian Product（デカルト積）** を生成します。\n\n**【爆発の計算式】**\n- 1万行 × 1万行 ＝ 1億行\n- 10万行 × 10万行 ＝ 100億行（即死レベル）\n- 1000万行 × 1000万行 ＝ 宇宙の終わり\n\n### 📍 解決策・予防策\n- 意図的に行う特別な用途（1行の定数テーブルとのJOINなど）以外、**必ず意味のあるビジネスキーで `INNER JOIN` / `LEFT JOIN` をする**。\n- `EXPLAIN` の中に `CartesianProduct` という単語が見えたら、即座にクエリを停止させてレビューし直すこと。\n\n---\n\n## 2. 致命的な原因②：Data Skew (データの偏り・スキュー)\n\n「クラスター全体のリソースは空いているのに、ジョブがずっと99%で止まっている」。\nこれは、一見するとシステム側の不具合に見えますが、大抵は**Data Skew**が原因です。\n\n**【Data Skewとは何か？】**\n特定の `id` （例えば異常値である `NULL` や、デフォルト値 `\"Web\"`、大口顧客のIDといった、「極端に大量のレコードを持つ値」）で `GROUP BY` や `JOIN` を行うと発生します。\n\nSparkなどの分散処理エンジンは、データの種類（キーのHash値）ごとに各サーバー（CPUコア）へタスクを分散させます。\n\n\\`\\`\\`mermaid\ngraph TD\n    classDef safe fill:#10b981,stroke:#fff,stroke-width:2px,color:#fff;\n    classDef danger fill:#ef4444,stroke:#fff,stroke-width:2px,color:#fff;\n\n    A[Shuffle / JOIN 処理] --> B[Core 1: 50MB]:::safe\n    A --> C[Core 2: 120MB]:::safe\n    A --> D[Core 3: 15GB 💥]:::danger\n    \n    D -.-> E((OOM Crash / 処理の塩漬け))\n\\`\\`\\`\n\n### 📍 解決策・予防策 (モダンなアプローチ)\nもしあなたが Databricks のような最新環境を使っているなら、小手先のコード修正ではなく **AQE (Adaptive Query Execution)** に頼るのが最適です。\n\n```sql\n-- 【処方箋】AQEのスキュー最適化オプションをONにする（通常はデフォルトでON）\nSET spark.sql.adaptive.enabled = true;\nSET spark.sql.adaptive.skewJoin.enabled = true;\n```\nAQEは、実行途中に「特定のキーだけ異常にデカい」と悟ると、そのキーのためだけにタスクを自動分割して並列処理に切り替えてくれます。\n\n---\n\n## 3. パフォーマンス改善の小技集（チートシート）\n\nインフラのアップグレードを打診する前に、以下の3つは必ず試してください。\n\n### 3.1 事前にフィルタリング (Predicate Pushdownの補佐)\n`JOIN` する対象が巨大なログテーブルの場合、結合してから `WHERE` で年月などを絞るのではなく、**事前にCTE等で対象年代だけのサブセットを作ってからJOINする**のが鉄則です。（Data Skippingの活用）\n\n### 3.2 Broadcast JOIN の強制\n結合する片方のテーブルが非常に小さい場合（数万行程度までのマスタなど）、ヒントを使用して強制的に全ノードにブロードキャスト（コピー配布）させます。これにより、高コストな「シャッフル通信」を完全に回避できます。\n\n```sql\nSELECT /*+ BROADCAST(dim_table) */ * \nFROM fact_table \nJOIN dim_table ON fact_table.item_id = dim_table.item_id\n```\n\n### 3.3 OPTIMIZE と ZORDER (物理配置の最適化)\n頻繁に `WHERE` や `JOIN` に使われる列（日付やユーザーIDなど）がある場合、ストレージ上でその列が近くに集まるよう、物理的にデータを並べ替えておきます。\nこれにより、クエリ実行時に「関係ないファイルを開く」時間を劇的に短縮（数十倍の高速化）できます。\n\n```sql\n-- Delta LakeにおけるZORDER最適化\nOPTIMIZE my_logs_table ZORDER BY (user_id, event_date);\n```\n\n---\n\n## 4. 確認クイズ（理解度チェック）\n\n### Q1. トラブルシューティングの際、クエリの先頭に `EXPLAIN FORMATTED` をつけると何が出力されますか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: クエリの実行計画（クエリプラン）が出力されます。**\n\nデータベースエンジンがそのSQLをどう解釈し、どの順番でファイルを読み、どのJOIN方式（Hash/SortMerge/Broadcast）を選択し、どこで結合するかの「設計図」がテキストで表示されます。ボトルネックの特定には必須のコマンドです。\n</details>\n\n### Q2. データの偏り（Skew）の原因になりやすい代表的な値は何ですか？\n\n<details>\n<summary>💡 回答を見る</summary>\n\n**正解: `NULL`、またはシステム上のデフォルト値です**\n\n特に大量のログが「未登録ユーザー（user_id = NULL）」で送られてきている場合、`GROUP BY user_id` や `JOIN` を行うと NULL が一つのCPUコアに全集中してスパイクします。\n`WHERE user_id IS NOT NULL` で先に切り捨てるか、ダミーのランダムIDを振って分散させる（Salting手法）などの前処理が必要です。\n</details>\n"
};
